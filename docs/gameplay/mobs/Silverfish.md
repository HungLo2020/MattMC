# Silverfish

The **Silverfish** is an aggressive mob in MattMC.

## Obtaining

Silverfish can be encountered through its normal spawning rules, structures, events, or content-specific systems when those systems are available. For creative testing and mapmaking, use the [Silverfish Spawn Egg](../items/SilverfishSpawnEgg.md).

## Behavior

Silverfish is classified as aggressive here: it is hostile, uses hostile targeting, or has player-targeting behavior in its implementation.

## Notes

* This mob is registered as `minecraft:silverfish`.
* Its spawn egg is registered as `minecraft:silverfish_spawn_egg`.
* Its entity registration uses `MobCategory.MONSTER`.
* Its entity class is `Silverfish`.
* Its registered size is `0.4, 0.3` blocks.
* This mob comes from the base MattMC/Minecraft entity set.
