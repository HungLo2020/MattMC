# Mule

The **Mule** is a passive mob in MattMC.

## Obtaining

Mule can be encountered through its normal spawning rules, structures, events, or content-specific systems when those systems are available. For creative testing and mapmaking, use the [Mule Spawn Egg](../items/MuleSpawnEgg.md).

## Behavior

Mule is classified as passive here: it does not normally start fights with players. Any special interactions, taming, breeding, bucket, mount, or utility behavior still follows its in-game implementation.

## Notes

* This mob is registered as `minecraft:mule`.
* Its spawn egg is registered as `minecraft:mule_spawn_egg`.
* Its entity registration uses `MobCategory.CREATURE`.
* Its entity class is `Mule`.
* Its registered size is `1.3964844, 1.6` blocks.
* This mob comes from the base MattMC/Minecraft entity set.
