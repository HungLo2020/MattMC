# Hoglin

The **Hoglin** is an aggressive mob in MattMC.

## Obtaining

Hoglin can be encountered through its normal spawning rules, structures, events, or content-specific systems when those systems are available. For creative testing and mapmaking, use the [Hoglin Spawn Egg](../items/HoglinSpawnEgg.md).

## Behavior

Hoglin is classified as aggressive here: it is hostile, uses hostile targeting, or has player-targeting behavior in its implementation.

## Notes

* This mob is registered as `minecraft:hoglin`.
* Its spawn egg is registered as `minecraft:hoglin_spawn_egg`.
* Its entity registration uses `MobCategory.MONSTER`.
* Its entity class is `Hoglin`.
* Its registered size is `1.3964844, 1.4` blocks.
* This mob comes from the base MattMC/Minecraft entity set.
