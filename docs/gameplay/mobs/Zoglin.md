# Zoglin

The **Zoglin** is an aggressive mob in MattMC.

## Obtaining

Zoglin can be encountered through its normal spawning rules, structures, events, or content-specific systems when those systems are available. For creative testing and mapmaking, use the [Zoglin Spawn Egg](../items/ZoglinSpawnEgg.md).

## Behavior

Zoglin is classified as aggressive here: it is hostile, uses hostile targeting, or has player-targeting behavior in its implementation.

## Notes

* This mob is registered as `minecraft:zoglin`.
* Its spawn egg is registered as `minecraft:zoglin_spawn_egg`.
* Its entity registration uses `MobCategory.MONSTER`.
* Its entity class is `Zoglin`.
* Its registered size is `1.3964844, 1.4` blocks.
* This mob comes from the base MattMC/Minecraft entity set.
