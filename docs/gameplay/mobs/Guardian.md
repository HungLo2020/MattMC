# Guardian

The **Guardian** is an aggressive mob in MattMC.

## Obtaining

Guardian can be encountered through its normal spawning rules, structures, events, or content-specific systems when those systems are available. For creative testing and mapmaking, use the [Guardian Spawn Egg](../items/GuardianSpawnEgg.md).

## Behavior

Guardian is classified as aggressive here: it is hostile, uses hostile targeting, or has player-targeting behavior in its implementation.

## Notes

* This mob is registered as `minecraft:guardian`.
* Its spawn egg is registered as `minecraft:guardian_spawn_egg`.
* Its entity registration uses `MobCategory.MONSTER`.
* Its entity class is `Guardian`.
* Its registered size is `0.85, 0.85` blocks.
* This mob comes from the base MattMC/Minecraft entity set.
