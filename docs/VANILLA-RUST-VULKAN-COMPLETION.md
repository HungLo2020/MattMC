# Vanilla Rust Vulkan completion ledger

Updated 2026-09-13, through r835 adult unmarked brown-horse inventory entity-preview acceptance, with r836-r839 remaining base coats and r840-r842 remaining marking layers prepared and test-validated but awaiting serial GPU capture. Powered-creeper and powered-wither energy swirl plus the invulnerable-wither base variant now have explicit contracts and strict Current/Frozen fixture evidence, and remain pending real paired proof. Goal 1 remains incomplete. This ledger covers the full requested scope; it is not a percentage estimate or a family-wide acceptance certificate. Historical acceptance below is indexed by PROGRESS.md and must be replayed against the current implementation before final completion. Missing reconciliation means unknown, not necessarily unimplemented.

## Completion rule

Every scoped capability needs a normal end-to-end Rust Vulkan run, equivalent deterministic Frozen **Java OpenGL** captures, passing unchanged parity checks, semantic resource/submission/completed-presentation ownership evidence, and bounded resource behavior. Neither Java Vulkan implementation is a correctness baseline. Source availability, unit tests, private diagnostic admission, and sampled process RSS alone cannot satisfy this rule. Distant Horizons and Iris shader-pack execution are excluded; absence of their GPU-state contamination remains required.

## Full scope and outstanding proof

| Required area | Current evidence / source entry point | Remaining work |
| --- | --- | --- |
| Terrain, fluids, translucency | GameRenderer calls LevelRenderer.enqueueRustGalStaticTerrainForWholeFrame; RustGalTerrainRenderer and native vulkanic/terrain contain implementation | Reconcile accepted terrain/fluids fixtures with normal admission, sorting/material variants, reloads and retirement; implement uncovered cases |
| GUI, HUD, text | RustGalGuiRenderer, WorldTextSemanticCollector; native gui_frontend and gui_mesh_frontend; retained GUI diaries listed in PROGRESS | Reconcile each GUI/HUD/text path, clipping/blending/fonts and resource variants with original paired evidence |
| Entities and layers | RustGalWorldPrimitiveRenderer.modelMeshRenderSemantics; entity/model submissions. The adult and baby unarmored white Horse, baby white-coat marking-and-saddle grid, baby diamond-, copper-, iron-, gold-, netherite-, and dyed-leather-armored white Horses, and ordinary tamed unchested and chested adult and baby Donkey and Mule now have exact body and equipment source gates for their distinct entity identities, age-selected models, chest geometry, textures and saddle layers in Current and Frozen, plus Rust composition and cross-repository fixture contracts. | Capture and accept these world fixtures; remaining armor and state combinations are explicitly incomplete; inventory other model and layer restrictions and match each to evidence |
| Block entities | All 27 registered types are source-inventoried as 23 render-state families: 22 use the common semantic dispatcher and piston uses the separate moving-block traversal; the ABI contract fails on unreviewed new registrations. Enchanting-table, book-bearing lectern, front-text and yellow-glowing back-text oak signs, crossed-chain, attached vertical-chain and wall-plank text-bearing oak hanging signs, SAVE-mode Structure Block with its exact 12-edge bounding box and all four invisible-cell overlay classes, CLEARED, running, FINISHED-success, required-failure and registry-backed optional-failure Test Instances with correlated box/beam/error-volume/see-through-text composition, all four indexed-item families (Campfire, Shelf, Brushable and Vault), static and east-struck floor Bell, the south-facing Copper Golem Statue across all four poses, all four oxidation textures, and waxed/unwaxed registered blocks, a complete two-half red Bed, closed upward and fully opened east-facing purple Shulker Boxes plus the undyed default-material branch, the full static undecorated Decorated Pot cardinal grid plus four-distinct-sherd and active positive/negative-wobble representatives with exact seven-part ModelPart compositions, inactive base-shell, stage-five breaking base-shell, active closed-eye and fully framed hunting/open-eye Conduit representatives, plain standing white and red Banners, one- and two-pattern white Banners, and a complete cardinal-facing white Wall Banner grid with their exact model compositions, stable standing Skeleton, Wither Skeleton, Zombie and Creeper heads, powered live-animation Dragon and Piglin heads, the complete cardinal-facing Skeleton Wall Skull grid, and stable north-facing Wither Skeleton, Zombie and Creeper wall heads, and powered live-animation Dragon and Piglin wall heads, plus resolved-profile standing and wall Player heads with exact semantic textures, pig mob spawner, non-ominous and ominous waiting trial spawners, stabilized active trial spawner, layered End Portal, and active full-height End Gateway plus beam now have equivalent Current/Frozen fixtures with Current Rust completion and passive Frozen producer contracts. Standing-sign atlas-backed Model, Conduit ModelPart and standing Skeleton Skull direct-texture destruction fixtures now select and fail-closed verify any exact vanilla stage 0 through 9; stage 5 remains the default source gate. Dynamic Model/ModelPart mesh keys have one-frame-grace, generation-guarded retirement with bounded mock and real headless Vulkan replacement proof. | Capture and accept these pairs; add indexed-item/state/animation variants and remaining representative families, and reconcile paired destruction pixels across selected stages, resource variants, sustained gameplay, reload and presentation behavior |
| Items and first-person hands | Historical normal shields r628 and static grid r670/r672/r675/r676/r679/r680/r683/r684; normal special-item cases r661/r663/r664/r665/r666/r667/r668 | Preserve exact accepted scope; reconcile other item types, foil variants, hands and source identities. Private moving compass/recovery evidence is not fresh normal acceptance |
| Particles | GameRenderer particle collection and world primitive frontend | Reconcile particle families, atlas animation, transparency/depth and lifecycle evidence |
| Sky, clouds, weather | Explicit GameRenderer enqueueRustGalSky/Clouds/WeatherForWholeFrame calls | Reconcile dimensions, weather/cloud modes, celestial/background variants and paired captures |
| World effects | WorldRenderRoutePolicy contains outline/crack/border routes; harness has flame/shadow/leash/beam controls | Trace each effect's producer, material, native submission and accepted fixture; cover missing variants |
| Post-processing | RustGalFrameCoordinator has semantic post-effect execution entry | Enumerate vanilla effects and pass/resource contracts; verify each actual execution and transition |
| Resource packs | Existing GUI/terrain pack scenarios and item/atlas diaries; recent scoped reload captures | Reconcile replacements, animation, font/model/material changes and repeated reload retirement across all consumers |
| Menus and loading | RustGalPanoramaRenderer and GUI/frame coordinator exist | Trace startup/loading/menu/world-entry/exit presentation, resize and resource ownership with real evidence |
| Every remaining vanilla callsite | GameRenderer collection leads to RustGalFrameCoordinator.executeWholeFrameVulkan | Build producer-to-semantic-to-native coverage inventory, including rejected/disabled branches; prove no silent omissions |
| Exclusive architecture | WorldRenderRoutePolicy.usesJavaCompatibility refuses selected Vulkan; native gal/resources/sync and Vulkan backend exist | Audit all paths for Java renderer/policy ownership, shared handles, borrowed Iris state and second presentation; scoped receipts cannot certify unobserved paths |
| Bounded resources and stability | Scoped accepted RSS receipts and native lifecycle tests are retained. Dynamic Model, ModelPart and item keys now retire after one idle grace frame through exact-generation transactions; a 24-generation crumbling replacement loop remains at one live mesh/resource/texture on mock and real headless Vulkan and balances teardown. | Verify repeated reload, resize/fullscreen, world transitions, shutdown, submission completion and GPU/native retirement under sustained gameplay; confirm the new dynamic-residency and retirement-backlog metrics in real captures |

Paths above refer to `src/main/java/net/minecraft/client/renderer`, `src/main/java/net/vulkanic`, and `src/main/rust/render/vulkanic`. The broad rows are intentionally unverified until their original evidence and current gates are reconciled.

## Armor implementation and remaining proof

The controlled four-piece diamond fixture has original full private acceptance for base/stopped foil (r690/r691/r692) and two naturally timed moving phases (r707/r709). The moving report revalidates the first-phase manifest, all five native submissions and completed presentations, exact seven-part geometry and simulation steps, every paired clock within16, independently inspected Frozen pixels, and visible matching glint changes on all four pieces. These are scoped fixture proofs, not normal armor admission or whole-game lifetime certification.

The per-face lighting defect is implemented and verified in those moving captures. Armor decal trims additionally require Equal depth comparison with depth writes, as declared by Frozen. ABI58 now carries explicit entity-mesh depth policy3; Rust creates the corresponding pipeline and Java copies the private armor declaration. All1741 native tests pass (2 ignored), including actual coincident/nearer/farther Vulkan pixels, depth-write descriptor checks and world/hand FFI transport. Current Java world/bridge/equipment selection787tests passes. The private inline decal-trim fixture subsequently passed original paired acceptance in r720; the private ordinary registered gold/spire fixture subsequently passed in r722.

The shared opt-in fixture supports registered gold/spire trims and a codec-supported inline gold/spire decal pattern. All bundled trim patterns declare decal=false, so the inline case is required to exercise Equal depth. Readiness and receipts distinguish material, asset, decal and registry/inline identity. Frozen renderer behavior is unchanged. Shared Frozen equipment tests passed20/20 before the first trimmed capture. r710 then produced all twelve native base/trim/foil draws but remains rejected: unaligned clocks and one interpolated model age fail strict evidence checks. Shared capture readiness now waits for the observed settled age; r711 verifies all five exact paired poses. A bounded shared CPU sprite observer preserves independent Frozen trim hashes and UV/resource/depth metadata. Its first Current run also observed the count-only coverage traversal; the Current diagnostic hook now excludes that explicit traversal while retaining the exact four-source requirement. r713 proves all five native bound-atlas sprite hashes/UVs against the independent Frozen CPU sources and verifies the four actual producer observations. A bounded encoding cache follows texture ownership and is invalidated on accepted animation updates;1743 native tests pass. The full trim verifier and timing preflight now compose that source proof with existing ownership, exact geometry, clock and pixel checks.36 equipment and557 harness Python tests pass. Fresh r720 original paired acceptance now passes all five poses, including native bound-atlas source correspondence, exact geometry and completed ownership evidence. Probe maxima1/2/1/1/1 and paired clock distances12/16/16/16/4 satisfy unchanged limits. Both clients exited0; all ten PNGs were visually inspected Frozen-first and retained. This proves only the private inline decal fixture. The Current-only opt-in bounded wait reads the actual natural clock afterwards; both repositories use ordinary maxFps260/vsyncfalse. Latest full harness suite558 passes; latest native suite1743 passes/2 ignored. Refer to PROGRESS.md for detailed evidence and remaining lifecycle limitations.

Next: broader entity-preview variants beyond the accepted inventory, horse and SmithingScreen cases; actual damaged-wolf translucent crack overlays; other dyed/multiple equipment layers and orthographic cases; resource retirement and normal admission. Moving inventory glint is accepted in r776/r777, and static inventory decal-trim Equal depth is accepted in r774 below. Keep all armor normally gated until the required evidence is complete. Full-scope coverage reconciliation remains outstanding, so neither a completion percentage nor a reliable time estimate is supported.

## Current source restrictions inspected during r707

These are concrete routing restrictions, not a claim that every listed input has a reachable vanilla producer. The remaining inventory must identify those producers and their gameplay cases before declaring coverage complete.

- `RustGalWorldPrimitiveRenderer.modelPartMeshEligibilityReason` now admits bounded destruction overlays and copies them as explicit projected Rust crumbling meshes. It still rejects sheeted geometry with foil, non-shield foil without a private flag, and animated non-block-atlas/non-shield sprites. The current vanilla callsite inventory contains no sheeted-plus-foil ModelPart producer: Shield submits `sheeted=false`, and selected-Vulkan Trident uses its separate direct-texture base-plus-glint route before the legacy ModelPart fallback. `SubmitNodeCollection.submitModelPartInternal` consumes this classifier and throws when the Rust whole-frame route has no eligible mesh. Reconcile modded callers and future vanilla additions before removing the defensive rejection.
- `modelMeshRenderSemantics` still gates all layered equipment behind `mattmc.dev.rustArmorFoil`; `standaloneModelFoilIneligibility` only recognizes the private armor contract and its explicit trident family. The new moving verifier does not remove these gates.
- The main whole-frame `GameRenderer` path explicitly collects terrain, weather, sky, clouds, particles/model particles, cracks, world border, indexed meshes and first-person work before `RustGalFrameCoordinator.executeWholeFrameVulkan`. This establishes entry points for the coverage inventory; it does not prove every variant reaches a supported native draw.
- `WorldRenderRoutePolicy.Route.usesJavaCompatibility` refuses compatibility after Vulkan selection or during Rust whole-frame ownership. This is useful local architectural evidence; the whole-path ownership audit remains required.
- Native Fabulous routing checks mesh strata/material modes and specific external material writers (`terrain_handoff_material_quads_are_supported`). Enumerate reachable rejected combinations and resource-pack external post-effect graphs. Do not infer they are all missing from comments: the inspected code already has explicit external-target execution paths.

## Additional layer prerequisites found through r842

Wolf crack overlays and spin-attack semantic tint now copy neutral -1 separately from outline color, matching Frozen callsites; regression tests pass, but actual gameplay acceptance is outstanding. World energy swirl previously divided already-normalized ModelPart UVs again; the extraction now applies offsets directly and the real cube-traversal regression passes. Its dedicated Rust material matches vanilla's one-plus-one additive blend, `0.1` alpha cutoff, emissive/fullbright color, LEQUAL depth test with depth writes and no culling. The actual GAL pipeline descriptor is covered by a focused Rust test, while selected shader-pack material lowering rejects the family rather than flattening it into `gbuffers_textured`. Direct assembly and the Fabulous/G-buffer forward phase both have command-order regressions proving the base mesh precedes the depth-writing shell. Real server-authored powered-creeper and powered-wither scenarios plus strict Current route/execution and Frozen passive base-plus-swirl observations are implemented. A separate real invulnerable-wither fixture requires full health, no powered layer, more than 80 invulnerability ticks and the exact `wither_invulnerable.png` base emission, avoiding vanilla's later blinking interval. The obsolete generic translucent wither-armor admission was removed. Vanilla EnergySwirl has `OutlineProperty.NONE`, so forwarded outline colors no longer reject the layer and the base mesh remains the sole outline-mask producer. An opt-in paired glowing fixture now verifies replicated glow state in both repositories; Current completed-submission receipts must correlate one base outline mesh instance and positive shell quads one-to-one with every capture. A prepared lifecycle gate also requires completed normal reload receipts, two post-reload presentations, one positive replacement mesh generation, bounded mesh keys across all correlated captures, and post-reload shell execution for powered fixtures. Actual runtime satisfaction remains outstanding. The world route remains behind `mattmc.dev.rustEnergySwirl` until paired pixels and native ownership/completion satisfy these gates. No Java fallback is introduced.

## Ordinary trim reference r721

Frozen Java OpenGL independently completed all five registered gold/spire poses with healthy exit0. All five PNGs were inspected before any Current ordinary-trim capture; image hashes, predeclared probes, natural clocks and four LEQUAL/write sprite observations per pose are retained in frozen-full-pose-reference.json. The original aggregate remains rejected for unsupported trim scope, so this is reference evidence only. Native atlas-source validation now explicitly supports ordinary LEQUAL/write draws and rejects missing/extra membership or mismatched depth;36 equipment tests pass. Composed fixture, pipeline, pixel and timing support plus fresh paired acceptance remain required.

The ordinary-trim composed verifier now includes exact registry components, native atlas/pipeline membership, independent r721 anchors and phase targeting.37 equipment Python tests,25 Java equipment tests and558 full harness tests pass. The actual Frozen reference document and all five anchors validate without changing its original rejected manifest. Fresh r722 original full paired manifest now passes: both clients exit0, five native atlas/source/ownership checks, exact geometry, probe maxima1/2/1/1/1 and paired clock distances12/12/4/8/8. All ten images were inspected Frozen-first and retained. This accepts only the private registered gold/spire fixture; damaged wolf/translucent, dyed/multiple layers, orthographic equipment and general retirement/admission remain unproven.

## Wolf armor prerequisite through r723

Both repositories now have equivalent ordinary wolf/body-armor setup and exact replicated readiness. Frozen-only r723 completed five high-crack front-view captures with damage51/max64 on an adult pale wolf; all were inspected and retained before Current wolf images. Original full report remains rejected for missing native/model/paired proof. Typed fixture and independent pixel checks exist with unchanged RGB<=2 limits, but only supplement the still-missing selected-draw, texture/material, exact-pose and ownership evidence. NONE/LOW/MEDIUM states, other views, dyes/foil/baby variants and lifetimes remain unproven.

## Authored model order prerequisite through r729

Fresh r734 subsequently passes its original full private wolf high-crack paired manifest. All five stepped front poses match independent Frozen pixels and exact model/packed geometry; native bound resources, persistent accepted upload bytes and submission IDs, and completed exclusive Rust presentation are verified. Both clients exited0, all ten images were inspected Frozen-first, and complete process-memory/recovery evidence is retained. This proves only adult pale wolf HIGH51/64 armor with vanilla reload. Other crack states, views, baby/dyed/foil variants, other equipment materials and general lifetimes remain outstanding; normal armor is still gated.

Through r732, all five actual Current captures additionally pass exact packed-source geometry correspondence against independent Frozen r730 (all80 vertex bytes and396 indices), actual geometry buffer/range/index bindings and raster state, plus the composed selected owner/instance/texture/pose/pixel checks. These remain supplemental because persistent geometry upload contents and fresh full original acceptance are still unproven. Normal armor remains gated.

The subsequent selected-input composition passes all five retained r729/r728 poses: native frame/submission IDs agree with completed owner/ACK/capture receipts, semantic and actual uploaded instance matrices match Frozen float32 inputs, and material/sampler/section checks pass alongside exact paired CPU poses and existing texture/order checks. Ten wolf Python tests pass, including evidence-substitution rejection. This is supplemental retained-run evidence; complete native mesh geometry and cull correspondence, full harness integration and fresh original paired acceptance still remain. No armor admission follows.

Frozen r728 actual draw-call observation confirms wolf body/cracks/armor order, while Current r727 originally executed body/armor/cracks and failed region pixels. ABI59 now carries optional signed model collection order from the Java semantic extraction boundary into Rust batching, without adding order to GPU resource keys. Focused real Vulkan overlap tests pass;800 selected Java tests pass. Native full run1746passed/1stale layout-count assertion/2ignored; corrected layout assertion passes its focused rerun. Current r729 now matches all five exact CPU poses, all fixed pixel probes (zero error), and region mean RGB approximately0.062/0.066/0.073. Bounded decoded native textures, actual declared pipeline/order and float32 view correspondence also pass against independent Frozen evidence. These remain supplemental: the combined native geometry/upload/sampler/owner/ACK verifier and fresh original paired wolf acceptance are outstanding. No normal armor admission follows from this result.

## Resumed source coverage reconciliation through r736

r736 passes its original full paired LOW6/64 wolf manifest and all five scoped checks. Ten images were inspected Frozen-first and hashed; complete process-memory receipts and verified recovery are retained. HIGH and LOW front-view scopes now have private acceptance. NONE/MEDIUM, other applicable variants, lifetimes and normal armor admission remain outstanding. Older prerequisite paragraphs above describe historical evidence; r734/r736 supersede their high/low acceptance gaps only.

These concrete producer paths were re-read in the current worktree. This is a starting inventory, not exhaustive coverage or new runtime acceptance:

| Producer / trigger | Semantic route and current restriction | Required next evidence or implementation |
| --- | --- | --- |
| EquipmentLayerRenderer equipment layers and glint; WolfArmorLayer cracks | submitModelSemantic / direct texture semantics -> modelMeshRenderSemantics; armor_cutout_no_cull, armor_translucent and armor_decal_cutout_no_cull return unavailable unless rustArmorFoil is set (RustGalWorldPrimitiveRenderer.java:8263). Armor glint independently gates at standaloneModelFoilIneligibility:8633. | Complete remaining materials, applicable variants and resource lifetimes, then fresh normal-mode paired admission. Private wolf acceptance cannot remove this whole-family gate. |
| CreeperPowerLayer / WitherArmorLayer when powered | EnergySwirlLayer submits an animated entity-model layer. The private route preserves normalized animated UVs, exact additive/emissive/cutout/depth state, fixture entity identity, execution receipts, and direct plus Fabulous/G-buffer base-before-shell ordering. Real powered-creeper and powered-wither fixtures have strict Current/Frozen producer evidence; a separate full-health invulnerable-wither fixture proves the distinct base texture without a powered shell. Vanilla excludes the shell from outlines. The prepared glowing fixture requires equivalent replicated state and exactly one base outline-mask instance beside positive shell work on every Current capture. Reload generation/reuse is separately gated. Selected shader-pack use remains rejected. | Run strict paired powered-creeper, powered-wither and invulnerable-wither captures after GLX recovery, including the glowing powered variants, then satisfy lifecycle and normal admission. |
| ModelPart submissions with sheeted foil, crumbling, non-admitted foil or animated non-owned atlas | modelPartMeshEligibilityReason:8060 explicitly rejects these combinations. TridentSpecialRenderer uses a separate direct-texture model plus glint route, so its legacy sprite-less branch is not by itself a Vulkan coverage defect. | Enumerate reachable special-item/block-entity callers and actual arguments before counting missing cases; match accepted shield/trident scopes to their real routes. |
| Whole-frame scene assembly | GameRenderer:1172–1222 collects terrain, weather, sky, clouds, particles, cracks, border and indexed features; :1371 calls executeWholeFrameVulkan. | Map each producer/material/variant to native execution and original evidence. Presence of these entry points does not establish family-wide completion. |
| r736 hotbar slot two | Visually, Frozen contains a thin horizontal item mark absent from Current. Wolf-only probes do not examine this slot. | First verify equivalent inventory/components and GUI state from independent receipts; then trace selected GUI item geometry and native draw if inputs match. Cause is unknown; do not change Frozen behavior or broaden the wolf pass. |

Next implementation priority after this inventory: finish wolf NONE/MEDIUM references using the existing strict proof machinery, while investigating the newly visible hotbar discrepancy. Expand the inventory across the remaining broad rows before assigning a whole-goal percentage.

The subsequent saved-inventory check identifies r736 slot two as `minecraft:springfield1873`, with TACZ ammunition custom data; its item asset selects `minecraft:tacz_gun`. The verified recovery inventory confirms canonical level.dat is unchanged. This is a custom gun, so the visible discrepancy does not establish a missing vanilla item. Runtime item receipt and exact custom GUI cause remain unproven; retain the observation separately from vanilla completion requirements. Evidence: r736/hotbar-saved-item-identification.json.

## Medium wolf private acceptance r738

Fresh r738 original paired manifest passes all five medium-damage wolf captures, including exact CPU and packed geometry, actual accepted uploads/resources and completed exclusive Rust presentation. All ten images were inspected Frozen-first; all fixed probes have zero error and region RGB means remain below0.064 versus limit2. Both clients exited0; complete memory and verified recovery evidence are retained. This adds only adult pale wolf MEDIUM32/64 front-view stepped captures and vanilla reload to the private HIGH/LOW evidence. NONE0/64 requires independently observed two-draw body/armor membership; broader equipment and normal admission remain incomplete.

## Shared equipment producer inventory (source inspected during r740)

EquipmentClientInfo.LayerType declares 17 types. All following producers feed EquipmentLayerRenderer, whose base armor material remains normally gated. This enumerates reachable source families; it does not claim every variant needs a separate redundant capture or that existing historical evidence is absent.

| Layer type(s) | Producer |
| --- | --- |
| HUMANOID, HUMANOID_LEGGINGS | HumanoidArmorLayer |
| WINGS | WingsLayer, including player texture substitution and adult/baby models |
| WOLF_BODY | WolfArmorLayer, including damage overlay |
| LLAMA_BODY | LlamaDecorLayer, including trader llama decoration and adult/baby models |
| HORSE_BODY, HORSE_SADDLE | HorseRenderer -> SimpleEquipmentLayer |
| DONKEY_SADDLE, MULE_SADDLE | DonkeyRenderer -> SimpleEquipmentLayer |
| PIG_SADDLE | PigRenderer -> SimpleEquipmentLayer |
| STRIDER_SADDLE | StriderRenderer -> SimpleEquipmentLayer |
| CAMEL_SADDLE | CamelRenderer -> SimpleEquipmentLayer |
| ZOMBIE_HORSE_SADDLE, SKELETON_HORSE_SADDLE | UndeadHorseRenderer -> SimpleEquipmentLayer |
| NAUTILUS_BODY, NAUTILUS_SADDLE | NautilusRenderer / ZombieNautilusRenderer -> SimpleEquipmentLayer |
| HAPPY_GHAST_BODY | HappyGhastRenderer -> SimpleEquipmentLayer |

SimpleEquipmentLayer selects adult/baby geometry and forwards its authored starting order. EquipmentLayerRenderer resolves dye, optional player texture and each declared layer, increments authored order, emits foil on the first visible layer only, and appends trim. Bundled equipment/leather.json provides a concrete remaining multilayer case: dyed leather base plus untinted leather_overlay for humanoid and leggings, versus a single dyed horse layer. Next equipment material work should exercise this shared multilayer/tint contract, followed by reconciliation of the listed producers and lifetime proof before normal admission. Iris context updates in the inspected producer are guarded against semantic submission and Vulkan selection; whole-path architectural verification remains required.

The ordinary warm adult `STRIDER_SADDLE` branch now has a strict source gate. Both repositories author and verify the same replicated visible, adult, non-suffocating Strider with a real saddle-slot item. Frozen passively records the canonical warm body and `textures/entity/equipment/strider_saddle/saddle.png` submissions after ordinary OpenGL model emission. Current retains the owning `minecraft:strider` identity for both meshes, distinguishes their completed composition by exact texture, and the harness requires the saddle route, projected mesh, correlation and one completed body-plus-saddle Rust presentation. The Current launcher uses the existing private equipment switch only for this fixture. Source validation passes with Current/Frozen production compilation, 334 production contracts and 741 Python harness tests. This is not normal admission: paired pixels, actual exclusive presentation evidence, reload/resource-pack variants and bounded replacement/retirement proof remain blocked by the current GLX context failure.

The ordinary adult `PIG_SADDLE` branch now has the equivalent strict source gate. Both repositories author and verify the same replicated visible adult Pig with a real saddle-slot item. Frozen passively records the canonical temperate body and `textures/entity/equipment/pig_saddle/saddle.png` submissions after ordinary OpenGL model emission. Current retains the owning `minecraft:pig` identity for both meshes, distinguishes their completed composition by exact texture, and the harness requires the saddle route, projected mesh, correlation and one completed body-plus-saddle Rust presentation. The Current launcher uses the existing private equipment switch only for this fixture. Source validation passes with Current/Frozen production compilation, 335 production contracts and 744 Python harness tests. This remains private source readiness: paired pixels, actual exclusive presentation evidence, reload/resource-pack variants and bounded replacement/retirement proof remain blocked by the current GLX context failure.

The stable standing adult `CAMEL_SADDLE` branch now has the equivalent strict source gate. Both repositories author and verify the same replicated visible adult Camel with a real saddle-slot item while requiring standing pose, no rider, no dash and zero jump cooldown. Frozen passively records the canonical body and `textures/entity/equipment/camel_saddle/saddle.png` submissions after ordinary OpenGL model emission. Current retains the owning `minecraft:camel` identity for both meshes, distinguishes their completed composition by exact texture, and the harness requires the saddle route, projected mesh, correlation and one completed body-plus-saddle Rust presentation. The Current launcher uses the existing private equipment switch only for this fixture. Source validation passes with Current/Frozen production compilation, 336 production contracts and 747 Python harness tests. This remains private source readiness: paired pixels, actual exclusive presentation evidence, sitting/ridden/dashing variants, reload/resource-pack variants and bounded replacement/retirement proof remain required.

The adult tamed `NAUTILUS_BODY` plus `NAUTILUS_SADDLE` branches now have one combined strict source gate. Both repositories author and verify the same replicated visible adult tamed Nautilus with real diamond body armor and saddle slot items. Frozen passively records the canonical body, `textures/entity/equipment/nautilus_body/diamond.png`, and `textures/entity/equipment/nautilus_saddle/saddle.png` submissions after ordinary OpenGL model emission. Current retains the owning `minecraft:nautilus` identity for all three meshes, distinguishes the equipment layers by exact texture, and the harness requires three correlated route/model receipts plus two distinct completed body-plus-layer Rust compositions. The Current launcher uses the existing private equipment switch only for this fixture. Source validation passes with Current/Frozen compilation, 337 production contracts and 750 Python harness tests. This remains private source readiness: paired pixels, actual exclusive presentation evidence, other armor materials, baby and zombie-nautilus variants, reload/resource-pack variants and bounded replacement/retirement proof remain required.

The temperate adult Zombie Nautilus now has an equivalent strict source gate through its distinct `ZombieNautilusRenderer` body path and the shared `NAUTILUS_BODY` plus `NAUTILUS_SADDLE` layers. Both repositories author the exact temperate registry variant, adult visible state, diamond body armor and saddle. Frozen passively records `textures/entity/nautilus/zombie_nautilus.png` plus both equipment textures after ordinary OpenGL model emission. Current requires all three correlated meshes under `minecraft:zombie_nautilus` and two distinct completed body-plus-layer compositions. Source validation passes with Current/Frozen compilation, 338 production contracts and 753 Python harness tests. Paired pixels, the warm coral model, baby normal Nautilus, other armor materials, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The warm adult Zombie Nautilus now covers the separate coral model and texture branch with the same strict equipment composition gate. Both repositories bind the exact `ZombieNautilusVariants.WARM` registry variant and require `ZombieNautilusCoralModel`, visible adult state, diamond body armor and saddle. Frozen passively records `textures/entity/nautilus/zombie_nautilus_coral.png` plus both equipment textures after ordinary OpenGL model emission. Current requires all three correlated meshes under `minecraft:zombie_nautilus` and two distinct completed body-plus-layer compositions. Source validation passes with Current/Frozen compilation, 339 production contracts and 756 Python harness tests. Other armor materials, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The baby tamed Nautilus now covers the distinct `ModelLayers.NAUTILUS_BABY` body geometry and `textures/entity/nautilus/nautilus_baby.png` texture branch while retaining the shared `NAUTILUS_BODY` and `NAUTILUS_SADDLE` equipment models. Both repositories author and verify the same visible baby tamed state with real diamond body armor and saddle items. Frozen passively records the baby body plus both equipment textures after ordinary OpenGL model emission. Current requires all three correlated meshes under `minecraft:nautilus` and two distinct completed body-plus-layer compositions. Source validation passes with Current/Frozen compilation, 340 production contracts and 759 Python harness tests. Other armor materials, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult tamed copper-armored Nautilus now covers the exact `minecraft:copper_nautilus_armor` item-to-`textures/entity/equipment/nautilus_body/copper.png` asset mapping while retaining the ordinary adult body and saddle submissions. Both repositories author and verify the same visible adult tamed state with real copper body armor and saddle items. Frozen passively records all three ordinary OpenGL model emissions; Current requires three correlated meshes and both completed body-plus-layer compositions under `minecraft:nautilus`. Source validation passes with Current/Frozen compilation, 341 production contracts and 762 Python harness tests. Gold and netherite Nautilus armor, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult tamed iron-armored Nautilus now covers the exact `minecraft:iron_nautilus_armor` item-to-`textures/entity/equipment/nautilus_body/iron.png` asset mapping while retaining the ordinary adult body and saddle submissions. Both repositories author and verify the same visible adult tamed state with real iron body armor and saddle items. Frozen passively records all three ordinary OpenGL model emissions; Current requires three correlated meshes and both completed body-plus-layer compositions under `minecraft:nautilus`. Source validation passes with Current/Frozen compilation, 342 production contracts and 765 Python harness tests. Netherite Nautilus armor, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult tamed gold-armored Nautilus now covers the exact `minecraft:golden_nautilus_armor` item-to-`textures/entity/equipment/nautilus_body/gold.png` asset mapping while retaining the ordinary adult body and saddle submissions. Both repositories author and verify the same visible adult tamed state with real golden body armor and saddle items. Frozen passively records all three ordinary OpenGL model emissions; Current requires three correlated meshes and both completed body-plus-layer compositions under `minecraft:nautilus`. Source validation passes with Current/Frozen compilation, 343 production contracts and 768 Python harness tests. Paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult tamed netherite-armored Nautilus now covers the exact `minecraft:netherite_nautilus_armor` item-to-`textures/entity/equipment/nautilus_body/netherite.png` asset mapping while retaining the ordinary adult body and saddle submissions. Both repositories author and verify the same visible adult tamed state with real netherite body armor and saddle items. Frozen passively records all three ordinary OpenGL model emissions; Current requires three correlated meshes and both completed body-plus-layer compositions under `minecraft:nautilus`. This completes source-level gates for the five canonical adult Nautilus armor materials: diamond, copper, iron, gold and netherite. Source validation passes with Current/Frozen compilation, 344 production contracts and 771 Python harness tests. Paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The ordinary adult saddled Horse now has a strict source gate through its world entity renderer path, separate from the already accepted HorseInventoryScreen previews. Both repositories author and verify a visible adult tamed `minecraft:horse` with the exact white variant, no markings, an empty body-armor slot and an actual saddle item. Frozen passively records the correlated `textures/entity/horse/horse_white.png` base and `textures/entity/equipment/horse_saddle/saddle.png` layer after ordinary OpenGL model emission. Current requires both correlated meshes and the completed body-plus-saddle composition under `minecraft:horse`; the private launcher admits only this exact fixture. Source validation passes with Current/Frozen compilation, 345 production contracts and 774 Python harness tests. Other coats, markings, armor and baby variants, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult creamy unmarked saddled Horse extends the strict world entity path beyond the white coat. Both repositories author and verify a visible tamed adult with `Variant.CREAMY`, `Markings.NONE`, an empty body slot, and an actual saddle. Frozen passively records correlated `horse_creamy.png` and shared `horse_saddle/saddle.png` emissions; Current requires their correlated adult-model meshes and completed Rust composition. Source validation corrected a generated nonexistent creamy-specific saddle path and passes with both Java compile tasks, 401/401 production contracts, and 942/942 Python harness tests. Paired pixels, exclusive presentation, the remaining adult coat and marking variants, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The remaining adult unmarked base coats complete the seven-coat saddled Horse source grid through exact `CHESTNUT`, `BROWN`, `BLACK`, `GRAY`, and `DARK_BROWN` fixtures alongside white and creamy. Each fixture requires a visible tamed adult, `Markings.NONE`, an empty body slot, and a real saddle. Current selects the exact coat texture and requires correlated adult body and shared saddle meshes with completed Rust composition; Frozen passively records the matching two ordinary OpenGL emissions. Source validation corrected cloned receipt keys, coat-specific Java contract identities, adult age diagnostics, and coat texture selection. Both Java trees compile, production contracts pass 406/406, and the complete graphics harness passes 957/957. Paired pixels, exclusive presentation, adult marking combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult white-coat `WHITE_DOTS` fixture begins the adult marking matrix. It requires a visible tamed adult with `Variant.WHITE`, `Markings.WHITE_DOTS`, an empty body slot, and a saddle. Current requires correlated white-body, white-dots marking, and shared saddle meshes with completed body-and-marking and body-and-saddle Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from the matching entity and position. Exact receipts and equivalence checks reject baby state and bind all three resource identities. Both Java trees compile, production contracts pass 407/407, and the complete graphics harness passes 960/960. Paired pixels, exclusive presentation, the remaining adult coat-and-marking combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult white-coat `WHITE` fixture extends that matrix with the exact `horse_markings_white.png` layer while retaining adult white body, empty body slot, and saddle state. Current requires three correlated meshes and both completed Rust compositions; Frozen passively records the matching base, marking, and saddle emissions from one entity and position. The receipt and equivalence gates require adult age and the exact `WHITE` marking identity. Both Java trees compile, production contracts pass 408/408, and the complete graphics harness passes 963/963. Paired pixels, exclusive presentation, the remaining adult marking combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult white-coat `WHITE_FIELD` fixture adds the exact `horse_markings_whitefield.png` layer on the same adult body-and-saddle path. Current requires three correlated meshes and both completed Rust compositions; Frozen passively records the three matching emissions from one entity and position. Exact readiness and equivalence gates require adult age and `Markings.WHITE_FIELD`; focused validation caught and corrected an initially cloned `WHITE` readiness value. Both Java trees compile, production contracts pass 409/409, and the complete graphics harness passes 966/966. Paired pixels, exclusive presentation, the remaining adult marking combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult white-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching emissions from one entity and position. Both Java trees compile, production contracts pass 410/410, and the complete graphics harness passes 969/969. The adult white-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult creamy-coat `WHITE_DOTS` fixture begins the next adult marking row. It requires `Variant.CREAMY`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_creamy.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three emissions. Integrated validation explicitly changes and checks the inherited base receipt, preventing a white-coat mesh from satisfying the creamy fixture. Both Java trees compile, production contracts pass 411/411, and the complete graphics harness passes 972/972. Paired pixels, exclusive presentation, the remaining adult creamy marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact creamy adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching emissions from one entity and position. Exact equivalence checks reject alternate marking state. Both Java trees compile, production contracts pass 412/412, and the complete graphics harness passes 975/975. Paired pixels, exclusive presentation, the remaining adult creamy marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` while retaining exact creamy adult, empty-body-slot, and saddled state. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching emissions from one entity and position. Both Java trees compile, production contracts pass 413/413, and the complete graphics harness passes 978/978. Paired pixels, exclusive presentation, adult creamy `BLACK_DOTS`, the broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact creamy adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 414/414, and the complete graphics harness passes 981/981. The adult creamy-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult chestnut-coat `WHITE_DOTS` fixture begins the third adult marking row. It requires `Variant.CHESTNUT`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_chestnut.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three ordinary OpenGL emissions. Exact fixture and integrated structural checks reject baby state, alternate coat or marking identity, and inherited white or creamy coat textures. Both Java trees compile, production contracts pass 415/415, and the complete graphics harness passes 984/984. Paired pixels, exclusive presentation, the remaining adult chestnut marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact chestnut adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 416/416, and the complete graphics harness passes 987/987. Paired pixels, exclusive presentation, the remaining adult chestnut marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` while retaining exact chestnut adult, tamed, empty-body-slot, and saddled state. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 417/417, and the complete graphics harness passes 990/990. Paired pixels, exclusive presentation, adult chestnut `BLACK_DOTS`, the broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact chestnut adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 418/418, and the complete graphics harness passes 993/993. The adult chestnut-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult brown-coat `WHITE_DOTS` fixture begins the fourth adult marking row. It requires `Variant.BROWN`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_brown.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three ordinary OpenGL emissions. Exact fixture and integrated structural checks reject baby state, alternate coat or marking identity, and inherited white, creamy, or chestnut coat textures. Both Java trees compile, production contracts pass 419/419, and the complete graphics harness passes 996/996. Paired pixels, exclusive presentation, the remaining adult brown marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult brown-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact brown adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 420/420, and the complete graphics harness passes 999/999. Paired pixels, exclusive presentation, the remaining adult brown marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult brown-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` on the same exact brown adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 421/421, and the complete graphics harness passes 1002/1002. Paired pixels, exclusive presentation, adult brown `BLACK_DOTS`, the broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult brown-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact brown adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 422/422, and the complete graphics harness passes 1005/1005. The adult brown-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult black-coat `WHITE_DOTS` fixture begins the fifth adult marking row. It requires `Variant.BLACK`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_black.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three ordinary OpenGL emissions. Exact fixture and integrated structural checks reject baby state, alternate coat or marking identity, and inherited brown-coat texture. Both Java trees compile, production contracts pass 423/423, and the complete graphics harness passes 1008/1008. Paired pixels, exclusive presentation, the remaining adult black marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult black-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact black adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 424/424, and the complete graphics harness passes 1011/1011. Paired pixels, exclusive presentation, the remaining adult black marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult black-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` on the same exact black adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 425/425, and the complete graphics harness passes 1014/1014. Paired pixels, exclusive presentation, adult black `BLACK_DOTS`, the broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult black-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact black adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 426/426, and the complete graphics harness passes 1017/1017. The adult black-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult gray-coat `WHITE_DOTS` fixture begins the sixth adult marking row. It requires `Variant.GRAY`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_gray.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three ordinary OpenGL emissions. Exact fixture and integrated structural checks reject baby state, alternate coat or marking identity, and inherited black-coat texture. Both Java trees compile, production contracts pass 427/427, and the complete graphics harness passes 1020/1020. Paired pixels, exclusive presentation, the remaining adult gray marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult gray-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact gray adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 428/428, and the complete graphics harness passes 1023/1023. Paired pixels, exclusive presentation, the remaining adult gray marking identities and broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult gray-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` on the same exact gray adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 429/429, and the complete graphics harness passes 1026/1026. Paired pixels, exclusive presentation, adult gray `BLACK_DOTS`, the broader cross-product, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult gray-coat `BLACK_DOTS` fixture completes the four non-none marking identities for that adult coat. It binds `Markings.BLACK_DOTS` to `horse_markings_blackdots.png` while retaining the exact gray adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 430/430, and the complete graphics harness passes 1029/1029. The adult gray-coat marking row is source-complete; paired pixels, exclusive presentation, the remaining adult coat-and-marking cross-product, resource variants, and bounded lifetime proof remain required, and normal equipment admission stays closed.

The adult dark-brown-coat `WHITE_DOTS` fixture begins the seventh and final adult marking row. It requires `Variant.DARK_BROWN`, `Markings.WHITE_DOTS`, adult tamed state, an empty body slot, and a saddle. Current selects `horse_darkbrown.png` and requires it with `horse_markings_whitedots.png` and the shared saddle in correlated meshes and both completed compositions; Frozen passively records the same three ordinary OpenGL emissions. Exact fixture and integrated structural checks reject baby state and alternate coat or marking identity. Both Java trees compile, production contracts pass 431/431, and the complete graphics harness passes 1032/1032. Paired pixels, exclusive presentation, the remaining adult dark-brown marking identities, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult dark-brown-coat `WHITE` fixture adds `horse_markings_white.png` on the same exact dark-brown adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 432/432, and the complete graphics harness passes 1035/1035. Paired pixels, exclusive presentation, dark-brown `WHITE_FIELD` and `BLACK_DOTS`, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult dark-brown-coat `WHITE_FIELD` fixture adds `horse_markings_whitefield.png` on the same exact dark-brown adult body-and-saddle path. Current requires the three correlated meshes and both completed Rust compositions; Frozen passively records the matching ordinary OpenGL emissions from one entity and position. Exact fixture and equivalence checks reject alternate coat, marking, and age state. Both Java trees compile, production contracts pass 433/433, and the complete graphics harness passes 1038/1038. Paired pixels, exclusive presentation, adult dark-brown `BLACK_DOTS`, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult dark-brown-coat `BLACK_DOTS` fixture completes the final adult marking row and therefore the seven-coat by four-marking adult Horse cross-product at the source-gate level. It binds `Variant.DARK_BROWN` and `Markings.BLACK_DOTS` to `horse_darkbrown.png` and `horse_markings_blackdots.png` while retaining exact adult, tamed, empty-body-slot, and saddled state. Current requires all three correlated meshes and both completed Rust compositions; Frozen passively records the three matching ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 434/434, and the complete graphics harness passes 1041/1041. Paired pixels, exclusive presentation, resource variants, remaining non-horse rendering scope, and bounded lifetime proof remain required, and normal equipment admission stays closed.

A follow-up selector audit corrected a gap in Current's deterministic source evidence. `expectedModelMeshTextureId()` had exact arms for the recently added adult rows but omitted three creamy adult scenarios and most non-white baby marking scenarios, so those diagnostic scenarios could report the default coat despite exact entity state. Current now maps the complete 48-scenario non-white adult/baby coat-by-marking set to the matching vanilla coat resource. A production contract enumerates six coats, four markings, and both ages and requires every scenario on its exact texture arm. Current compiles and production contracts pass 435/435; the unchanged graphics harness remains 1041/1041. Frozen rendering and observation behavior were not changed. This repair strengthens source evidence but does not replace paired pixels, ownership, presentation, resource-variant, or bounded-lifetime acceptance.

The ordinary adult diamond-armored Horse extends that world entity path through the `HORSE_BODY` layer while retaining the saddle layer. Both repositories author and verify the same visible adult tamed white unmarked `minecraft:horse` with actual `minecraft:diamond_horse_armor` and saddle items. Frozen passively records the correlated base, `textures/entity/equipment/horse_body/diamond.png` armor and `textures/entity/equipment/horse_saddle/saddle.png` emissions after ordinary OpenGL model rendering. Current requires all three correlated meshes and both completed body-plus-layer compositions under `minecraft:horse`; the private launcher admits only this exact fixture. Source validation passes with Current/Frozen compilation, 346 production contracts and 777 Python harness tests. Other horse armor materials, coats, markings and baby variants, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The adult creamy diamond-armored Horse begins explicit non-white coat-by-armor coverage. `horse-creamy-diamond-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual diamond body armor, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the diamond armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. The integrated structural document rewrites both the inherited base mesh and route decision from white to creamy, so generic route validation cannot pass on the wrong coat. Both Java trees compile, production contracts pass 436/436, and the complete graphics harness passes 1044/1044. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy copper-armored Horse extends explicit non-white coat-by-armor coverage. `horse-creamy-copper-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual copper body armor, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the copper armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 437/437, and the complete graphics harness passes 1047/1047. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy iron-armored Horse extends explicit non-white coat-by-armor coverage. `horse-creamy-iron-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual iron body armor, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the iron armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 438/438, and the complete graphics harness passes 1050/1050. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy gold-armored Horse extends explicit non-white coat-by-armor coverage. `horse-creamy-gold-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual gold body armor, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the gold armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 439/439, and the complete graphics harness passes 1053/1053. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy netherite-armored Horse extends explicit non-white coat-by-armor coverage. `horse-creamy-netherite-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual netherite body armor, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the netherite armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 440/440, and the complete graphics harness passes 1056/1056. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult creamy dyed-leather-armored Horse completes the creamy coat-by-armor source row. `horse-creamy-dyed-leather-equipped` fixes `Variant.CREAMY`, `Markings.NONE`, adult tamed state, actual leather body armor dyed exactly `0x3366CC`, and a saddle. Current selects `horse_creamy.png` and requires its body mesh with the leather armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position while checking the same dye component. Both Java trees compile, production contracts pass 441/441, and the complete graphics harness passes 1059/1059. Paired pixels, exclusive presentation, the remaining coat-by-armor rows, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut diamond-armored Horse begins the second explicit non-white coat-by-armor row. `horse-chestnut-diamond-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual diamond body armor, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the diamond armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 442/442, and the complete graphics harness passes 1062/1062. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The deterministic baby Horse equipment setup now routes each scenario to its requested material. The diamond condition previously also admitted the baby copper, iron, gold, netherite, and dyed-leather scenarios, so their later branches could not execute in a real paired run. Current and Frozen now restrict that branch to baby diamond; the five other baby scenarios reach their matching material setup, including the exact `0x3366CC` leather dye. A production contract inspects the actual armor-assignment conditions and prevents the overlap from returning. Frozen compiles and production contracts pass 443/443; the unchanged complete graphics harness remains 1062/1062. Paired execution is still required before those scenarios can be accepted.

The adult chestnut copper-armored Horse extends the second explicit non-white coat-by-armor row. `horse-chestnut-copper-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual copper body armor, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the copper armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 444/444, and the complete graphics harness passes 1065/1065. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut iron-armored Horse extends the second explicit non-white coat-by-armor row. `horse-chestnut-iron-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual iron body armor, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the iron armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 445/445, and the complete graphics harness passes 1068/1068. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut gold-armored Horse extends the second explicit non-white coat-by-armor row. `horse-chestnut-gold-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual gold body armor, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the gold armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 446/446, and the complete graphics harness passes 1071/1071. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut netherite-armored Horse extends the second explicit non-white coat-by-armor row. `horse-chestnut-netherite-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual netherite body armor, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the netherite armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position. Both Java trees compile, production contracts pass 447/447, and the complete graphics harness passes 1074/1074. Paired pixels, exclusive presentation, the remaining coat-by-armor combinations, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The adult chestnut dyed-leather-armored Horse completes the second explicit non-white coat-by-armor row. `horse-chestnut-dyed-leather-equipped` fixes `Variant.CHESTNUT`, `Markings.NONE`, adult tamed state, actual leather horse armor dyed exactly `0x3366CC`, and a saddle. Current selects `horse_chestnut.png` and requires its body mesh with the leather armor and saddle meshes plus both completed Rust compositions; Frozen passively records the same three ordinary OpenGL emissions from one entity and position while checking the same dye component. Both Java trees compile, production contracts pass 448/448, and the complete graphics harness passes 1077/1077. Paired pixels, exclusive presentation, the remaining coat-by-armor rows, resource variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The ordinary adult copper-armored Horse adds the exact `minecraft:copper_horse_armor` item-to-`textures/entity/equipment/horse_body/copper.png` mapping on the same world entity path. Both repositories author and verify the same visible adult tamed white unmarked `minecraft:horse` with copper body armor and a saddle. Frozen passively records the correlated base, copper armor and saddle emissions after ordinary OpenGL model rendering. Current requires all three correlated meshes and both completed body-plus-layer compositions under `minecraft:horse`; the private launcher admits only this exact fixture. Source validation passes with Current/Frozen compilation, 347 production contracts and 780 Python harness tests. Iron, gold, netherite and leather horse armor, dye tint behavior, other coats, markings and baby variants, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The ordinary adult iron-armored Horse adds the exact `minecraft:iron_horse_armor` item-to-`textures/entity/equipment/horse_body/iron.png` mapping on the same world entity path. Both repositories author and verify the same visible adult tamed white unmarked `minecraft:horse` with iron body armor and a saddle. Frozen passively records the correlated base, iron armor and saddle emissions after ordinary OpenGL model rendering. Current requires all three correlated meshes and both completed body-plus-layer compositions under `minecraft:horse`; the private launcher admits only this exact fixture. Source validation passes with Current/Frozen compilation, 348 production contracts and 783 Python harness tests. Gold, netherite and leather horse armor, dye tint behavior, other coats, markings and baby variants, paired pixels, reload/resource-pack variants, exclusive presentation and bounded replacement/retirement proof remain required; normal equipment admission stays closed.

The ordinary adult gold-armored Horse adds the exact `minecraft:golden_horse_armor` item-to-`textures/entity/equipment/horse_body/gold.png` mapping on the same strict world path. Current requires the three correlated meshes and both completed body-plus-layer compositions; Frozen passively records the ordinary OpenGL emissions. Source validation passes with both compilations, 349 production contracts and 786 Python harness tests. Netherite and dyed leather with tint, paired pixels, reload/resource-pack variants, exclusive presentation and bounded lifetime proof remain required; normal equipment admission stays closed.

The ordinary adult netherite-armored Horse adds the exact `minecraft:netherite_horse_armor` item-to-`textures/entity/equipment/horse_body/netherite.png` mapping. Current requires all three correlated meshes and both completed compositions; Frozen passively records the ordinary emissions. Source validation passes with both compilations, 350 production contracts and 789 Python harness tests. This completes source gates for diamond, copper, iron, gold and netherite horse armor. Dyed leather and tint, paired pixels, reload variants, exclusive presentation and bounded lifetime proof remain required; normal equipment admission stays closed.

The ordinary adult dyed-leather Horse covers the sixth canonical `HORSE_BODY` material through the actual `minecraft:leather_horse_armor` item with `DYED_COLOR` RGB `0x3366CC`. Both capture trees require the exact replicated dye component. Current requires the canonical `horse_body/leather.png` mesh and saddle in completed Rust compositions; Frozen passively records the corresponding ordinary emissions. Source validation passes with both compilations, 351 production contracts and 792 Python harness tests. This completes source gates for all six horse-body materials. Paired pixels must still prove the tint result; reload/resource-pack variants, exclusive presentation and bounded lifetime proof also remain required, and normal equipment admission stays closed.

The baby diamond-armored white Horse now exercises the world renderer's age-selected `HORSE_BABY`, `HORSE_BABY_ARMOR`, and `HORSE_BABY_SADDLE` models together. Both capture trees author the same visible tamed baby with no markings, actual diamond body armor, and a saddle. Frozen passively records all three exact OpenGL emissions; Current requires the canonical white body, diamond armor, and Horse-saddle meshes with both completed Rust compositions. Source validation passes with both compilations, 361 production contracts and 822 Python harness tests. Paired pixels, exclusive presentation, resource variants, bounded lifetime proof, and the remaining baby armor and marking combinations remain required; normal equipment admission stays closed.

The baby copper-armored white Horse adds the exact `minecraft:copper_horse_armor` item and `textures/entity/equipment/horse_body/copper.png` resource to the same age-selected three-model world path. Frozen passively records the baby body, copper armor, and saddle emissions; Current requires their correlated meshes and both completed Rust compositions. Source validation passes with both compilations, 362 production contracts and 825 Python harness tests. Paired pixels, exclusive presentation, resource variants, bounded lifetime proof, and the remaining baby armor and marking combinations remain required; normal equipment admission stays closed.

The baby iron-armored white Horse adds the exact `minecraft:iron_horse_armor` item and `textures/entity/equipment/horse_body/iron.png` resource to the age-selected three-model world path. Frozen passively records all three ordinary emissions; Current requires their correlated meshes and both completed Rust compositions. Source validation passes with both compilations, 363 production contracts and 828 Python harness tests. Paired pixels, exclusive presentation, resource variants, bounded lifetime proof, and the remaining baby armor and marking combinations remain required; normal equipment admission stays closed.

The baby gold-armored white Horse adds the exact `minecraft:golden_horse_armor` item and `textures/entity/equipment/horse_body/gold.png` resource to the age-selected three-model world path. Frozen passively records all three ordinary emissions; Current requires their correlated meshes and both completed Rust compositions. Source validation passes with both compilations, 364 production contracts and 831 Python harness tests. Paired pixels, exclusive presentation, resource variants, bounded lifetime proof, and the remaining baby armor and marking combinations remain required; normal equipment admission stays closed.

The baby netherite-armored white Horse adds the exact `minecraft:netherite_horse_armor` item and `textures/entity/equipment/horse_body/netherite.png` resource to the age-selected three-model world path. Frozen passively records all three ordinary emissions; Current requires their correlated meshes and both completed Rust compositions. The unsupported-scenario guard now explicitly admits every baby armor fixture before setup. Source validation passes with both compilations, 365 production contracts and 834 Python harness tests. This completes the five untinted baby armor source gates. Paired pixels, exclusive presentation, the baby dyed-leather tint case, resource variants, bounded lifetime proof, and marking combinations remain required; normal equipment admission stays closed.

The baby dyed-leather white Horse completes the six-material baby `HORSE_BODY` source grid with an actual `minecraft:leather_horse_armor` stack carrying exact `DYED_COLOR` RGB `0x3366CC`. Frozen passively records the baby body, leather armor, and saddle emissions only after the item and dye state match; Current requires their correlated meshes and both completed Rust compositions. The shared verifier rejects wrong dye values and uses dyed-leather-specific failure statuses. Source validation passes with both compilations, 366 production contracts and 837 Python harness tests. Paired pixels must still prove tint output; exclusive presentation, resource variants, bounded lifetime proof, and marking combinations remain required, and normal equipment admission stays closed.

All Horse world fixtures now explicitly author the packed vanilla coat-and-marking state through the entity save/load path. Both Current setup paths and Frozen deterministic setup write `Variant.WHITE | Markings.NONE << 8` for the existing fixtures, preserving their required appearance while removing dependence on constructor defaults. This is the prerequisite for world marking-layer fixtures to select exact replicated marking state without reflection or renderer-side policy. Both repositories compile and the production contract remains 366/366.

The baby white-dots Horse uses that packed-state path for the first baby marking-layer world fixture. Both capture trees require the same visible tamed baby with `Variant.WHITE`, `Markings.WHITE_DOTS`, an empty body slot, and a saddle. Frozen passively records the white base, `textures/entity/horse/horse_markings_whitedots.png`, and saddle emissions. Current requires all three correlated meshes and separate completed base-plus-marking and base-plus-saddle compositions under `minecraft:horse`. An erroneous diamond-armor setup inclusion found during validation was removed from both trees. Source validation passes with both Java compile tasks, 367 production contracts, and 840 Python harness tests. Paired pixels, exclusive presentation, other baby marking types, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The remaining baby marking identities now extend that world fixture through `Markings.WHITE`, `WHITE_FIELD`, and `BLACK_DOTS`, with exact `horse_markings_white.png`, `horse_markings_whitefield.png`, and `horse_markings_blackdots.png` resources. Each fixture retains the baby white base, empty body slot, saddle, three correlated model routes, and separate base-plus-marking and base-plus-saddle composition receipts. Frozen observes only the matching ordinary OpenGL emissions. Final source inspection corrected malformed Frozen marking texture paths before completion. Source validation passes with both Java compile tasks, 370 production contracts, and 849 Python harness tests. This completes the four non-none baby marking identities at source level. Paired pixels, exclusive presentation, coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The remaining six baby Horse base coats now complete the seven-coat unmarked saddle grid. Exact fixtures cover `Variant.CREAMY`, `CHESTNUT`, `BROWN`, `BLACK`, `GRAY`, and `DARK_BROWN` with `horse_creamy.png`, `horse_chestnut.png`, `horse_brown.png`, `horse_black.png`, `horse_gray.png`, and `horse_darkbrown.png`. Each keeps `Markings.NONE`, an empty body slot, and a real saddle. Frozen passively records only the matching ordinary OpenGL body and saddle emissions; Current requires their correlated routes/models and a completed texture-specific Rust composition. Source validation passes with both Java compile tasks, 376/376 production contracts, and 867/867 Python harness tests. This completes the seven vanilla base-coat identities for the baby unmarked saddle path at source level. Paired pixels, exclusive presentation, coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The creamy-coat baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.CREAMY` with `Markings.WHITE_DOTS`, `Markings.WHITE`, `Markings.WHITE_FIELD`, or `Markings.BLACK_DOTS`, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_creamy.png`, the matching `horse_markings_whitedots.png`, `horse_markings_white.png`, `horse_markings_whitefield.png`, or `horse_markings_blackdots.png`, and saddle emissions. Current requires the same three correlated routes/models and separate completed coat-plus-marking and coat-plus-saddle Rust compositions. Full validation found and fixed an omitted baby-age predicate in both Current setup paths and the Frozen setup. It also corrected older white-field and black-dots verifier documents whose `clientVariant` incorrectly named the marking instead of the actual white coat. Source validation passes with both Java compile tasks, 380/380 production contracts, and 879/879 Python harness tests. Paired pixels, exclusive presentation, the remaining coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The chestnut-coat baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.CHESTNUT` with the exact requested marking, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_chestnut.png`, the matching marking texture, and saddle emissions. Current requires the same three correlated routes/models and separate chestnut-plus-marking and chestnut-plus-saddle Rust compositions. Source validation passes with both Java compile tasks, 384/384 production contracts, and 891/891 Python harness tests. Paired pixels, exclusive presentation, the remaining coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The brown-coat baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.BROWN` with the exact requested marking, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_brown.png`, the matching marking texture, and saddle emissions. Current requires the same three correlated routes/models and separate brown-plus-marking and brown-plus-saddle Rust compositions. Source validation passes with both Java compile tasks, 388/388 production contracts, and 903/903 Python harness tests. Paired pixels, exclusive presentation, the remaining coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The black-coat baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.BLACK` with the exact requested marking, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_black.png`, the matching marking texture, and saddle emissions. Current requires the same three correlated routes/models and separate black-plus-marking and black-plus-saddle Rust compositions. Source validation passes with both Java compile tasks, 392/392 production contracts, and 915/915 Python harness tests. Paired pixels, exclusive presentation, the remaining coat-and-marking cross-products, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The gray-coat baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.GRAY` with the exact requested marking, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_gray.png`, the matching marking texture, and saddle emissions. Current requires the same three correlated routes/models and separate gray-plus-marking and gray-plus-saddle Rust compositions. Source validation passes with both Java compile tasks, 396/396 production contracts, and 927/927 Python harness tests. Paired pixels, exclusive presentation, the remaining dark-brown coat-and-marking row, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

The dark-brown baby Horse row is source-complete for all four non-none marking identities: white dots, white, white field, and black dots. Both capture trees author `Variant.DARK_BROWN` with the exact requested marking, baby age, tamed state, an empty body slot, and a saddle. Frozen passively records exact `horse_darkbrown.png`, the matching marking texture, and saddle emissions. Current requires the same three correlated routes/models and separate dark-brown-plus-marking and dark-brown-plus-saddle Rust compositions. This completes exact source gates for the full seven-coat by four-non-none-marking baby Horse matrix. Source validation passes with both Java compile tasks, 400/400 production contracts, and 939/939 Python harness tests. Paired pixels, exclusive presentation, equipment combinations, reload/resource-pack variants, and bounded lifetime proof remain required; normal equipment admission stays closed.

## Undamaged wolf private acceptance r740

Original full paired manifest passes all five NONE0/64 poses with exactly body/armor draws, distinct exact packed geometry, accepted uploads, texture/material/sampler correspondence and exclusive completed Rust presentation. All twelve probes have zero error; region RGB means below0.00135 versus limit2. All ten images were inspected Frozen-first and hashed. Both clients exited0; complete memory and verified recovery/cleanup evidence are retained. All four damage states now have scoped adult pale wolf front-view private acceptance. General equipment variants and lifetimes remain incomplete; normal armor remains gated. Next concrete material prerequisite is ordinary dyed leather base plus untinted overlay; existing shared equipment fixture currently supports diamond only.

## Dyed leather prerequisite r741

Shared GraphicsAuditEquipmentFixture now supports explicitly requested leather-dyed material with ordinary leather items and DYED_COLOR RGB0x3366CC, exact replicated dye/item readiness and actual dye receipt fields. Existing default diamond receipt remains unchanged. Four fixture tests pass in each repository (Current19s, Frozen16s), including missing/wrong dye and slot substitution. Frozen-only five-pose enchanted/stepped reference r741 is running; no leather acceptance yet.

Current native equipment_capture observes layered instance uploads, pipeline and texture metadata generically, but exact source geometry bytes and accepted geometry upload checks are conditional on the wolf scope. Equipment validate_native also hardcodes four diamond bases and four foil draws. Extend the owned packed-geometry proof to equipment and require leather base/overlay texture, tint, authored order and first-layer-only foil membership; do not broaden the existing diamond proof by changing counts alone. Existing historical equipment geometry claims establish the documented pose/transform scope, not the stronger wolf-style packed-upload proof.

The r741 Frozen reference subsequently completed all five inspected poses with exact dye fixture and validated clocks/CPU simulation. Original aggregate remains rejected; independent unchanged-coordinate pixel anchors, memory and verified recovery are retained. Native equipment geometry observation now includes packed bytes, actual bindings and accepted upload proof for layered base/foil resources. Focused actual Vulkan rejection test passes; full native suite is running. Independent Frozen model-local packed-geometry source observation and exact leather layer/tint/order/texture composition remain prerequisites; no Current leather capture or normal admission yet.

The native full suite passed1751tests/2ignored. Shared CPU equipment observation now records actual post-setupAnim visible model-local geometry, tint/light/overlay and texture/pipeline declarations, plus separate actual ordinary draw-return order and copied pipeline/view/counts. Traversal and receipts are bounded and fail closed; they do not claim GPU completion.23 selected Java tests pass in each repository. Fresh Frozen-only r742 reference is running with these receipts. Native/CPU correspondence and full leather paired acceptance remain outstanding; normal armor remains gated.

Current r743 confirms layered ordering mismatch: four bases, four glints, four overlays versus Frozen r742 final glint flush after bases/overlays. First four native captures have complete accepted geometry proof; fifth fails missing accepted buffer upload proof, requiring bounded history/lifetime investigation. Paired natural clocks differ92/32/24/136/156 (>16), so observed pixel mismatches cannot yet isolate ordering alone. All five Current images were inspected; original failure retained. Actual uploaded tint lanes and exact native/Frozen geometry correspondence remain outstanding.13 Python fixture/CPU-contract mutation tests pass. No leather acceptance or normal admission follows.


## Leather verification and remaining model variants, r746 in progress

The bounded upload-history defect and deferred armor-glint ordering mismatch are fixed in Rust. The latest native full suite passes1754 tests with2 ignored. Shared ordinary Frozen CPU observations now match exact packed Current geometry, actual accepted uploads, five decoded vanilla texture sources, uploaded tint/pose/material, pipeline/sampler/depth and draw order. r744 and r745 provide supporting five-pose evidence only; their original manifests remain rejected. The integrated private leather verifier has24 focused passing tests and the full harness558 passes. Fresh paired r746 is running; its five Frozen images have been inspected and independently checked before Current. This paragraph does not declare paired acceptance or normal armor admission.

The next distinct geometry case is reachable through HumanoidArmorLayer.getArmorModel: state.isBaby selects babyModelSet, supplied separately by AbstractZombieRenderer. The current GraphicsAuditEquipmentFixture.ready explicitly rejects baby zombies. Extend the ordinary shared fixture with an explicit age selection and replicated age receipt, then obtain independent Frozen baby geometry/pixels before validating Current. Adult geometry references cannot prove that branch. InventoryScreen.renderEntityInInventory submits the ordinary entity state through the GUI path; RustGalWorldPrimitiveRenderer separately selects orthographic equipment layering and ARMOR_ORTHOGRAPHIC foil when that projection is active. Trace and verify this route with the actual equipped inventory entity before claiming orthographic coverage.

Reinspection confirms all17 declared equipment layer types have bundled asset entries. In particular, humanoid assets include chainmail/copper/diamond/gold/iron/leather/netherite/turtle_scute; horse_body includes six materials, and nautilus_body five. LLAMA_BODY has16 carpets plus trader decoration, HAPPY_GHAST_BODY16 harnesses, and the nine saddle types share saddle.json. These source inventories guide shared-contract and variant coverage; they do not convert the adult diamond, wolf or leather fixtures into family-wide proof. Player texture substitution, dye defaults/omitted layers, applicable baby models, view conventions, and resource retirement remain to be reconciled before removing the normal equipment gate.


## Private adult dyed leather acceptance r746

The fresh original full paired manifest passes all five stepped adult front captures and ordinary vanilla reload. All ten images were inspected Frozen-first. Each selected Current submission has12 draws matched to exact Frozen packed geometry and five independently decoded vanilla textures, expected tinted leather/untinted overlay/glint color, actual accepted geometry and instance uploads, pipeline/sampler/depth and final glint order, and completed exclusive Rust presentation. Exact CPU model/simulation/transform checks pass. Paired clock distances are8/12/12/12/8 (limit16); all12 probes per pose have maximum error1, and the maximum region RGB mean is0.050933 (limit2). Both clients exited0 with complete terminal memory observations; full inventories and archive payload/digest verification completed, followed by exact disposable-source cleanup with the canonical base retained. Original manifest and all image hashes are retained in inspected-paired-acceptance.json. This establishes the private adult leather0x3366CC layered fixture only; normal equipment admission, other variants and lifecycle coverage remain incomplete.


## Baby equipment prerequisite r747

The shared ordinary fixture now supports explicit graphicsAuditEquipmentAge=baby, uses Zombie.setBaby(true) only under that request, waits for replicated age equality, and records ageVariant plus actual isBaby. Default adult behavior/schema remains unchanged. Both repositories pass24 selected Java tests; combined Python equipment/age/full-harness tests pass573. Adult exact-schema acceptance and the report's explicit age gate reject baby evidence until independently validated. Frozen-only r747 is running to obtain baby model geometry and pixels; no baby acceptance follows from these source tests. Native renderer code is unchanged in this prerequisite.


The r747 Frozen baby reference completed all five inspected poses with isBaby=true, observed ageScale0.5, exact stepped ages and the expected12model/five-draw leather contract. Independent baby geometry, texture declarations and12pixel probes are retained separately from the adult reference. Its verified recovery is complete. Age-scoped local verification passes25 focused tests; the full558 harness passes and adult r746 still revalidates. Current-only baby diagnostic r748 is running; no actual Rust baby result or full paired age acceptance is claimed yet.


Current baby diagnostic r748 subsequently completed all five inspected captures. Scoped supporting comparison against r747 passes exact packed geometry/texture/tint/upload/order and completed ownership, CPU simulation/geometry, clock distances12/16/8/12/12 and probesmax1; regionRGBmeanmax0.067709 versus limit2. Both original one-sided manifests remain unchanged. Verified recovery is complete. The paired report now dispatches only the exact baby dyed-leather request to the age-specific full verifier;574 combined regression/harness tests pass. Fresh original paired r749 is running; whole baby scope and normal admission remain unproven until it completes.


## Private baby dyed leather acceptance r749

The original fresh full paired manifest passes all five baby-zombie captures, including ordinary simulation stepping and vanilla reload. Exact independent Frozen geometry,12selected Rust draws, source texture/tint/material/order and actual accepted uploads, completed exclusive Rust ownership, CPU transforms and baby age are verified. All ten images were inspected Frozen-first and hashed. Clock distances8/12/8/8/12 satisfy16; all12probes per pose have maximum error1 and maximum regionRGBmean0.061595 versus2. Both clients exited0 with complete terminal memory observations. Full inventories/archive payload and digest verification completed; the exact disposable source was removed with the canonical base and evidence retained. This adds only the documented baby dyed-leather front fixture; other equipment families/views/resources and normal admission remain incomplete.

The inventory renderer uses a separate GUI path, RustGalGuiRenderer.tryEnqueueEntityPip. Source inspection confirms EntityPipLayerCapture.order discards its argument, while model-submit signatures name the API tintedColor argument order and retain it as layerOrder; later mesh collection passes white. Glint is currently rejected by the GUI material/blend collector. It also retains mutable models until later collection, so actual setupAnim and visibility snapshot timing need verification. Obtain an ordinary equipped-player InventoryScreen Frozen reference and corresponding copied inputs before implementing and accepting this route. World armor evidence cannot prove this GUI implementation.


## Equipped inventory prerequisite r750

Shared GraphicsAuditInventoryEquipmentFixture authors ordinary leather equipment/dye on the integrated server, clears and saves the hotbar/offhand, waits for client replication, opens the normal InventoryScreen, and restores the saved inventory/options after capture completion. Both backends use their existing per-pose screenshot/acknowledgement protocol; the legacy external inventory screenshot flag is omitted to avoid racing world completion/restoration. A bounded read-only ModelFeatureRenderer observer copies the actual submitted armor pose/state and model-local geometry alongside draw declarations.22 selected Java tests pass in each repository, including pose-copy independence, bounds and fixture substitutions. Frozen-only r750 is running. No Rust inventory equipment acceptance follows; source correspondence, whole-preview body/material/depth/ordering/ownership and lifecycle validation remain required.


Inventory reference r750 completed one ordinary inventory capture with exact replicated dyed leather, but its saved diagnostic omitted modelGeometry because serialization checked only the world equipment property. The original receipt is preserved and verified recovery is complete. Shared serialization now includes inventory requests; lazy fixture item initialization fixes the resulting pre-bootstrap diagnostic regression. All23 selected Java tests pass in each repository. Fresh Frozen-only r751 is running to verify saved actual source geometry/poses; inventory GUI parity, native ownership and normal admission remain outstanding.

Source tracing during r751 identifies an additional inventory PIP mismatch: RustGalGuiRenderer.tryEnqueueEntityPip queues each result with a separate enqueueGuiMeshItemRequest(List.of(batch)), all layerIndex0. The scheduler assigns request sequences, whereas native GUI mesh grouping uses (stratum, sequence), requires contiguous layer indices, and shares offscreen color/depth plus a single composite within that group. The body and armor therefore need one semantic preview submission with copied layer data, preserving Rust ownership of pass execution. Also, ordinary GuiEntityRenderer uses overrideCameraAngle for camera orientation only, while Current additionally multiplies it into the model pose. These source findings still require actual reference/input/native parity validation.

Frozen r751 completed with client exit0 and one inspected ordinary equipped inventory image. The corrected saved geometry receipt is complete for8 armor model submissions, copied source poses/normals/state and4 actual completed armor draws; exact fixture and image/input hashes are retained. This excludes Avatar body observation and is not full GUI parity. Complete terminal memory receipts and verified six-file recovery/archive cleanup are retained. Current inventory preview implementation and actual paired native proof remain outstanding.

Inventory PIP grouping now submits one validated body/layer request and one scheduler token with contiguous layer indices, retaining full batch semantics. Existing Rust grouping supplies shared depth/color and one composite. A new Java regression exercises actual queued transport and preserves one sequence, layer membership, geometry/assets/materials; all79 GUI renderer tests pass. Existing native mock-GAL grouped-pass/composite test passes. No actual Current inventory acceptance follows: tint/order/state extraction, camera-pose correction, other materials and complete source/native paired evidence remain pending. Frozen is unchanged by this implementation.

Fresh Frozen r759 resolves the inventory view-fixture mismatch. Its captured background and render-return both use stored/supplied coordinates213/120, and all non-animation preview inputs exactly match retained Current r754. The inspected OpenGL image includes the exposed player skin; the bounded armor observer reports8 models and4 completed draws. Animation ages still differ392/447. Hashes,44 terminal memory samples and verified recovery/cleanup are retained. Current ABI60 r760 is prepared to test explicit entity-preview lighting and immediate grouped layer extraction. Body source/native geometry, armor no-cull policy, selected ownership, animation timing and paired acceptance remain outstanding.

Current r760 then matched every non-animation Frozen r759 preview input and improved the armor lighting, while the exposed player skin remained black or absent. The copied PlayerModel was being forced through the opaque GUI material even though its vanilla model RenderType is `entityTranslucent`; transparent outer-skin texels consequently wrote black and depth over the inner skin. ABI61 now transports explicit entity cutout/translucent no-cull materials. Java derives them from the actual RenderType pipeline, and Rust owns the two-sided cull, blend, depth-write and0.1 alpha-cutoff policy. This also supplies the previously missing armor-cutout no-cull policy. Full native1755pass/2ignored, Java GUI81/81, the added native policy regression and Java test compilation pass. r762 actual Current client exit0 and its inspected preview restores the face, hands and body gaps with zero non-animation input differences from Frozen r759; crop mean absolute RGB difference is1.61028/255. Complete memory and verified recovery evidence are retained. Inventory animation age, selected native body/layer correspondence, a strict paired acceptance run, atlas trims/glint and broader preview variants remain outstanding.

ABI62 adds bounded entity-preview result counters at the successful native whole-frame return: selected items, emitted raster layers, raster-plus-compose draws, semantic material mask, vertices and indices. Rust rejects a semantic item that mixes entity-preview lighting with another lighting family. The full native suite passes1759tests/2ignored and the selected Java suites pass535/535. r763 actual Current client exit0; submission950 is also the completed presented submission and reports1 preview item,9 raster layers,10 draws,768 vertices,1152 indices and mask384, exactly modes7 and8 for armor cutout no-cull and player translucent no-cull. Visual inspection retains the restored player skin and armor. Non-animation inputs exactly match Frozen r759; preview-crop mean absolute RGB difference is1.18272/255. Complete memory and verified recovery evidence are retained. This closes selected native body/layer correspondence for the base dyed-leather fixture. Animation age503 versus392, a strict paired acceptance run, atlas trims/glint and broader preview variants remain outstanding.

Fresh paired r764 closes the base dyed-leather InventoryScreen acceptance. Both actual clients exit0 and the original full paired manifest and visual report pass. Frozen and Current inputs match exactly outside ordinary age393/458; walk position, walk speed and death time are all zero, so age is a retained non-driving timing difference for this standing fixture. Full-frame mean RGB error is0.470/0.557/0.653 of255 and the preview-crop mean absolute RGB difference is1.47200/255. Current accepted/presented submission808 independently reports the expected1 preview item,9 native raster layers,10 draws, mask384,768 vertices and1152 indices. Both images, diff, input/native receipts, complete memory observations and verified recovery are retained. Base inventory preview correspondence and paired acceptance are complete. Foil/glint, armor trims and broader entity-preview variants remain outstanding.

Static armor foil is now explicit in the same entity-preview path. `EntityPipLayerCapture` accepts only the exact null-sprite `armorEntityGlint` submit, copies the armor glint texture, and transports Rust-owned standard-foil timing with the orthographic armor convention. Both Java and Rust reject perspective armor foil and require orthographic armor foil to use EntityPreview lighting with no item/block/decal layout. r765 deliberately preserved the first fail-closed result: the intermediate generic-lighting batch rejected the foil and the whole-frame route stopped on one unsupported PIP, with no Java same-frame fallback. After making the lighting and foil declaration atomic, full native1759pass/2ignored and selected Java91/91 pass. Fresh r766 passes its original strict paired manifest and visual report. Both inspected images show exposed skin, complete dyed leather armor and purple foil on all four slots. Full-frame mean RGB error is0.118/0.161/0.140 of255; preview-crop mean absolute RGB is1.32533/255. Non-animation inputs match exactly, while ordinary age400/489 remains non-driving in the standing fixture. Accepted/presented submission850 reports1 entity preview,13 raster batches,14 raster-plus-compose draws, mask400,1008 vertices and1512 indices. Complete terminal memory and verified recovery evidence are retained for both the failed diagnostic and accepted pair. This closes static zero-speed strength-0.5 inventory armor foil for the four-piece dyed-leather fixture. Moving foil, registered trims and broader entity-preview variants remain outstanding.

Static registered non-decal inventory trim is now explicit in the grouped entity-preview atlas path. The semantic sprite callback delegates into the same bounded model validator, the collector snapshots the stitched atlas rather than attempting a standalone sprite resource, and copied UVs expand through the sprite's atlas rectangle. r770 retains the pre-fix fail-closed result: Current rejected one unsupported PIP and performed no Java same-frame fallback. Fresh r772 passes its original strict paired manifest and visual report. Both inspected images show the complete gold spire trim over four dyed-leather pieces. Exact paired fixture receipts name `minecraft:gold`, `minecraft:spire` and decal=false. The paired CPU observations contain exactly three HUMANOID plus one HUMANOID_LEGGINGS 64x32 sprite, with identical pixel hashes, atlas positions/UV bounds and LEQUAL/write depth. Non-animation preview inputs match exactly; ordinary standing age392/456 remains non-driving. Full-frame mean RGB error is0.548/0.666/0.768 of255 and fixed preview-crop mean absolute RGB is2.49683/255. Accepted/presented submission875 reports1 preview,13 raster batches,14 draws, mask384,1008 vertices and1512 indices. Complete terminal memory and verified recovery/cleanup evidence are retained. Focused Current renderer/fixture tests pass88/88, corrected trim serialization passes2/2 in both repositories, and the latest unchanged native suite passed1757/2ignored. This closes only registered non-decal trim in the static inventory fixture. Decal trim still needs Equal depth through a distinct GUI material mode and ABI change; moving glint and broader preview variants remain outstanding.

ABI63 adds the distinct entity decal cutout/no-cull GUI material required by inline armor-trim patterns. Java recognizes only the exact armor decal pipeline, supplies EntityPreview lighting and0.1 cutout at batch creation, and validates that modes7/8/9 cannot carry generic lighting, item raster layout or foil. Rust decodes mode9, owns two-sided rasterization with disabled blending and Equal/write depth, and includes it in successful entity-preview submission counters. r773 retains the initial fail-closed integration result: stricter bridge validation rejected the collector's temporary generic-lighting batch, so Current stopped on one unsupported entity PIP without Java fallback. Its recovery archive and exact cleanup are complete. The collector now assigns preview lighting atomically, and fresh r774 passes its original strict paired manifest, exact workload comparison and visual report with both actual clients exit0. The exact paired fixture is four leather pieces dyed0x3366CC with gold spire trim, decal=true and registered=false. Both source receipts contain the same three HUMANOID and one HUMANOID_LEGGINGS 64x32 regions, atlas coordinates, UVs, pixel hashes and `EQUAL_DEPTH_TEST`/write policy. Non-animation preview inputs match; ordinary standing age395/461 remains non-driving. Full-frame mean RGB error is0.529/0.544/0.583 of255 and preview-crop mean absolute RGB is2.36276/255. Current submission969 is the completed presented submission and reports1 preview,13 batches,14 draws, material mask896,1008 vertices and1512 indices. Frozen memory peaks RSS7,253,012/HWM7,313,244KiB across46 terminal samples; Current peaks RSS5,526,028/HWM5,878,612KiB across41. Full native passes1758/2ignored, Current selected Java passes554 before and93 after the collection fix, and Frozen fixture/source tests pass7. r773/r774 both retain verified six-file recovery archives and exact disposable cleanup. This closes static inline decal trim for the inventory fixture; moving glint and broader preview variants remain outstanding. Next unused capture is r775.

Naturally moving inventory armor glint is now captured from the actual preview clock. The shared `foil-moving` fixture uses ordinary speed0.5/strength0.5. Current observes all four accepted orthographic armor-foil batches; Frozen records its ordinary armor-state clock. Alignment derives one Current target for the single preview and does not step simulation or override either clock. Fresh paired r776/r777 pass their original manifests at requested phases10000/40000: clock distances are12 and16 under the unchanged16-tick limit, both resource reloads and visual reports pass, all clients exit0, and Current validation is clean. Both image pairs were inspected Frozen-first. Within each phase, non-animation preview inputs match exactly; Current's completed presented result has1 preview,13 batches,14 draws, mask400,1008 vertices and1512 indices with exclusive Rust whole-frame ownership. A two-phase verifier additionally proves visible motion: armor-slot deltas change3641 Frozen and3643 Current pixels, with signed per-channel motion agreeing within0.02324 mean and3 maximum; player-preview crops also change11846/9820 pixels. Full native remains1758pass/2ignored; final Current Java timing/fixture tests pass24, Frozen23, and Python timing tests14. Complete process-memory receipts and verified recovery/cleanup are retained. This closes naturally moving glint for the authored four-piece dyed-leather InventoryScreen fixture. Broader preview variants and whole normal armor admission remain incomplete. Next unused capture is r778.

The second vanilla entity-preview producer now has a base acceptance. A shared capture-only fixture spawns a tamed adult white horse on the integrated server, waits for ordinary replication, opens the normal horse inventory through `ServerPlayer.openHorseInventory`, and records stable centered mouse/view inputs. Frozen renderer behavior is unchanged. Frozen-first r778 and Current diagnostic r779 established the source and native workload; fresh paired r780 passes its original manifest, exact fixture equivalence, visual report and vanilla reload with both clients exit0. Both images and their diff were inspected Frozen-first. All non-animation inputs match exactly; idle age2/79 remains non-driving with zero walk/death inputs. The fixed horse crop differs by0.24192 RGB levels on average and at most3. Current submission814 is also the completed presented submission and reports exactly1 entity preview,1 mode7 entity-cutout/no-cull batch,2 draws,288 vertices and432 indices, with Rust whole-frame ownership, same acquired/presented image and no Java Vulkan execution. Current validation is clean. Current selected Java passes26 and full native1758/2ignored; Frozen selected Java passes25; final combined Python regression passes577. Complete memory and verified recovery/cleanup evidence are retained. This closes only the unarmored, unmarked adult white-horse inventory preview. Horse markings/equipment/age variants and the third producer, SmithingScreen's armor stand, remain incomplete. Next unused capture is r781.

The third vanilla entity-preview producer now has a base acceptance. Exact `smithing` fixture mode opens the ordinary server-authorized `SmithingMenu`; shared diagnostics copy fixed view inputs plus the armor stand's visibility flags and six authored poses without changing Frozen rendering. r781 Frozen-first established the source and r782 Current-only established the native workload; fresh paired r783 passes its original manifest, exact fixture equivalence, visual report and vanilla reload with both clients exit0. Driving source inputs match exactly. The differing constructor yaw is confined to the hidden base plate, and wiggle6001 is outside its conditional renderer branch. The inspected armor-stand crop differs by0.11580 RGB levels on average and at most2. Current completed presentation submission831 reports1 preview item,1 mode7 batch,2 draws,216 vertices and324 indices with Rust whole-frame ownership and no Java Vulkan execution. Full native passes1758/2ignored; final combined Python regression passes581. Complete terminal memory and verified recovery/cleanup are retained. This closes only the unarmored adult normal-size smithing armor stand. Equipped result items and other stand/horse preview variants remain incomplete.

The ordinary equipped netherite-upgrade result extends that SmithingScreen acceptance in r790. Shared fixture code uses the server-authorized menu and vanilla recipe inputs, then requires the synchronized client result slot to contain a netherite chestplate. Shared read-only geometry observations cover the resulting armor model; Current observes its immutable copy during synchronous Rust entity-PIP collection. After removing the measured one-pixel padded-target offset and the hidden-base-plate yaw, Frozen and Current have exactly the same single netherite model and18 quads. Frozen independently records the expected 72-vertex/108-index LEQUAL/write/no-cull armor draw; Current performs no Java draw and completed presentation943 reports1 preview,2 batches,3 draws, mask128,288 vertices and432 indices under exclusive Rust whole-frame ownership. Fresh Frozen-first r790 passes the original paired manifest, exact fixture/inputs/geometry/ownership supplement, visual report and vanilla reload; crop mean error is0.13323/255 and maximum13 under the scoped max16 bound. Both clients exit0, Current validation is clean, complete process memory is retained, and recovery/cleanup is verified. Final Python discovery passes921, Current selected Java91 plus native1758/2ignored, and Frozen selected Java9. This closes the adult normal-size stand wearing this synchronized netherite chestplate result. Other result equipment and stand/horse variants remain incomplete. Next unused capture is r791.

The ordinary horse inventory preview with diamond horse armor and saddle passes fresh paired r794. Exact `horse-equipped` mode assigns both items through the spawned tamed adult white horse's ordinary equipment slots, requires their client replication, and opens the same server-authorized inventory as r780. The two equipment models match exactly by unique texture identity:72 diamond-armor quads and102 saddle quads. Frozen independently records their complete LEQUAL/write cutout/no-cull draw policies; Current records no Java draws and completed presentation933 reports1 preview,3 batches,4 draws, mask128,984 vertices and1476 indices under exclusive Rust ownership. The original paired manifest, fixture/inputs/geometry/owner supplement, visual report and vanilla reload pass; crop mean error is0.21858/255, with one raster-edge channel at66 and99.99th percentile3. Both images were inspected, both clients exit0, validation is clean, memory is complete, and recovery/cleanup is verified. Final Python discovery passes924, focused Java fixture/geometry and serializer checks pass in both repositories, and native remains1758/2ignored. This closes the adult white horse with diamond armor and saddle. Markings, other horse armor, baby horses and other equine variants remain incomplete. Next unused capture is r795.

The ordinary unarmored baby white-horse inventory preview passes fresh paired r798. The exact server fixture requires replicated baby state and empty body/saddle slots. Frozen r795 established the baked `HORSE_BABY` model: one72-quad white-horse mesh and one288/432 LEQUAL/write entity-cutout/no-cull draw. Current r796 revealed that the direct GUI copier skipped `AgeableMobRenderer`'s state-based model selection and therefore copied the adult model. A read-only semantic model selector now chooses the same baked variant without mutating renderer state. Current r797 proved the correction, and fresh Frozen-first r798 passes its original manifest, strict fixture/input/geometry/ownership supplement, visual report and vanilla reload. After the declared one-pixel guard normalization, all72 quads are identical outside ordinary non-driving idle age4/76. Current records no Java draw; completed presentation815 reports1 preview,1 batch,2 draws, mask128,288 vertices and432 indices under exclusive Rust whole-frame ownership. The inspected crop differs by0.07403/255 on average and at most3 under the strict mean0.25/max4 limit. Frozen memory peaks RSS8,138,100/HWM8,139,028KiB across47 samples; Current peaks RSS4,981,456/HWM5,194,268KiB across42. Final focused Current Java passes93, Frozen passes11, Python verifier/harness checks pass14, native remains1758/2ignored, and final full Python discovery passes927. This closes the unarmored baby white horse. Baby equipment/markings and other equine variants remain incomplete. Next unused capture is r799.

The equipped baby white-horse inventory preview passes fresh paired r800. Its server fixture requires baby state, diamond horse armor and saddle to replicate together. r799 established correct rendering but found duplicate base rows in the read-only Current receipt; the observer now runs only in base collection, and r800 contains exactly the white baby base72 quads, diamond baby armor72 and baby saddle102. Geometry matches Frozen by texture after the one-pixel guard normalization. Frozen independently records288/432 base,288/432 armor and408/612 saddle draws with LEQUAL/write, no cull and no blend; Current executes no Java draws. Completed presentation996 reports1 preview,3 batches,4 draws, mask128,984 vertices and1476 indices under exclusive Rust whole-frame ownership. The paired manifest, exact fixture/input/geometry/owner supplement, visual report and vanilla reload pass with both clients exit0; crop mean is0.06595 and max19 under mean0.25/max32. Frozen memory peaks RSS8,003,392/HWM8,008,144KiB across36 samples and Current RSS5,167,096/HWM5,219,200KiB across42. This closes diamond armor and saddle on the baby white horse. Markings, other armor and other equine variants remain incomplete. Next unused capture is r801.

Adult white-dot horse markings pass fresh paired r801. The shared capture-only fixture authors `Variant.WHITE | Markings.WHITE_DOTS << 8` through the horse's ordinary save/load path, then requires the exact variant and markings to replicate before opening the server-authorized inventory. Frozen independently records the adult white base and translucent white-dot layer as two72-quad models and two288/432 LEQUAL/write/no-cull draws; only the marking layer uses ordinary alpha blending. After Current's declared one-pixel PIP guard and non-driving idle age are normalized, both model sources are byte-for-byte equal. Current executes no Java draw and completed presentation986 reports1 preview,2 batches,3 draws, mask384,576 vertices and864 indices with the same acquired/presented image and exclusive Rust whole-frame ownership. The original manifest, exact fixture/input/geometry/ownership verifier, visual report and vanilla reload pass; the inspected crop mean is0.25147 and maximum3 under the scoped mean0.5/max4 bound. Both clients exit0, Current Vulkan validation is clean, process-memory receipts are complete, full Python discovery passes932, selected Java passes12 in each repository, native remains1758/2ignored, and recovery/cleanup is verified. This closes adult `WHITE_DOTS` only. Baby markings are next because they also exercise whether a layer uses the semantic-state-selected baby parent model. Next unused capture is r802.

Baby white-dot markings pass fresh paired r803 after retained diagnostic r802 exposed a real direct-preview model-selection defect. Frozen used identical baby geometry for base and markings, while Current used the baby base and adult marking model. The layer-parent interface now provides semantic-state model selection and `HorseMarkingLayer` requests it for the submitted horse state. In r803 every base/layer geometry hash matches Frozen's baby model, Current executes no Java draw, the original manifest and reload pass, and the crop differs by0.07689 mean/3 maximum under the strict mean0.25/max4 bound. Both memory receipts and verified recovery/cleanup are retained. r802 is not accepted. Next unused capture is r804.

Adult white-dot markings composed with diamond armor and saddle pass fresh paired r804. The exact replicated fixture produces four matched models and Frozen draw policies: base72, translucent markings72, saddle102 and diamond armor72 quads. Current executes no Java draws; completed presentation869 has1 preview,4 batches,5 draws,mask384,1272 vertices and1908 indices under exclusive Rust ownership. The original manifest, strict fixture/input/geometry/owner verifier, reload and equipped-adult pixels pass at mean0.22215/max66. Validation, memory and verified recovery/cleanup are complete. Next r805 covers the corresponding baby combination.

The corresponding baby composition passes fresh paired r805. Exact `horse-baby-marked-equipped` requires baby age, white `WHITE_DOTS`, diamond armor and saddle to replicate together. All four Current models match Frozen's baby geometry after the declared one-pixel guard: base72, translucent markings72, diamond armor72 and saddle102 quads. Frozen records that exact draw order and policy; Current executes no Java draws. Completed presentation945 reports1 preview,4 batches,5 draws,mask384,1272 vertices and1908 indices under exclusive Rust ownership. The original manifest, strict fixture/input/geometry/owner verifier, reload and equipped-baby pixels pass at mean0.06728/max19. Both clients exit0, Current validation is clean, memory is complete, and recovery/cleanup is verified. This closes the white-dot age/equipment combination grid. Other markings, coat variants, horse armor materials, donkey/mule/llama producers and broader entity-preview variants remain incomplete. Next unused capture is r806.

The distinct chested-equine producer starts with fresh paired r806. Exact `donkey-chested-equipped` creates a real donkey, equips its chest through vanilla slot499 so the inventory expands to five columns, equips the saddle slot, requires the complete state to replicate and opens the ordinary server-authorized HorseInventoryScreen. Frozen and Current match the complete84-quad chested donkey base and114-quad donkey-saddle model after the declared one-pixel guard; Frozen independently records saddle then base with their exact PIP offsets and LEQUAL/write/no-cull/no-blend policy. Current executes no Java draw. Completed presentation917 reports1 preview,2 batches,3 draws,mask128,792 vertices and1188 indices with exclusive Rust presentation and no Java Vulkan execution. The original manifest, strict fixture/input/geometry/owner verifier, reload and pixels pass at mean0.10024/max8 under the new mean0.25/max16 bound. Both clients exit0, validation and memory are clean, and recovery/cleanup is verified. Mule, llama, baby chested-equine variants and other equine equipment remain incomplete. Next unused capture is r807.

The distinct llama producer and decor layer pass fresh paired r808. Retained r807 first proved the fixture prerequisite: a new llama has strength zero, so even a chest yields zero storage columns and the ordinary screen cannot become ready. The corrected fixture uses the llama's vanilla save/load state path to set strength five before equipping the chest and blue carpet, and both clients require the complete replicated state. Frozen and Current match the creamy base66 quads and blue llama-body decor66 quads after the declared one-pixel guard. Frozen records the base at identity followed by decor at the ordinary0.001953125 offset, both LEQUAL/write/no-cull/no-blend; Current executes no Java draw. Completed presentation792 reports1 preview,2 batches,3 draws,mask128,528 vertices and792 indices with exclusive Rust presentation. The original manifest, strict fixture/input/geometry/owner verifier, reload and pixels pass at mean0.26387/max50 under the scoped mean0.5/max64 bound. Both clients exit0, validation and memory are clean, and recovery/cleanup is verified. Baby llama decor remains a distinct model-selection branch; mule, other variants and broader equine combinations remain incomplete. Next unused capture is r809.

The separate baby llama base and decor models pass fresh paired r810. r809 is retained as a healthy diagnostic whose model receipt was empty because the observer expected a chest flag that vanilla intentionally clears in baby `LlamaRenderState`; the entity's strength-five chest inventory remained real and complete. Correcting only that read-only predicate exposes two exact54-quad baby models in r810. Frozen records216/324 base and216/324 blue-decor draws with the ordinary decor depth offset; Current executes no Java draw and completed presentation931 reports2 native batches,3 draws,432 vertices and648 indices under exclusive Rust presentation. The strict baby crop passes at mean0.07543/max50 under mean0.25/max64. Both clients exit0, resource reload and validation pass, process memory is complete, and r809/r810 recovery and exact cleanup are verified. This closes only creamy blue-decor llamas across adult and baby model selection. Mule, other variants/colors and broader equine combinations remain incomplete. Next unused capture is r811.

The ordinary adult chested/saddled mule producer passes fresh paired r812. r811 retained healthy paired pixels and exact models/draws but lacked pose/state because the read-only observer's serialization condition omitted the new mule predicate; r812 follows the diagnostic-only correction. The real server-spawned mule replicates its chest, five storage columns and saddle before opening HorseInventoryScreen. Frozen and Current match mule saddle114 quads and chested mule base84 quads after the declared one-pixel guard. Frozen records saddle then base with the ordinary saddle offset and LEQUAL/write/no-cull/no-blend policy; Current executes no Java draw. Completed presentation821 reports2 batches,3 draws,792 vertices and1188 indices under exclusive Rust presentation. The strict crop passes at mean0.06414/max66 under mean0.25/max80. Both clients exit0, reload and validation pass, process memory is complete, and r811/r812 recovery and exact cleanup are verified. Adult chested donkey and mule producers are now covered; their baby variants and broader equine combinations remain incomplete. Next unused capture is r813.

The ordinary baby chested/saddled mule producer passes fresh paired r813. The real server-spawned baby mule replicates its chest, five storage columns and saddle before opening HorseInventoryScreen. Frozen and Current match every non-tail quad of the base84 and saddle114 models after the declared one-pixel guard; six ordinary age-driven tail quads differ across capture frames while matching exactly between the base and saddle layers within each client. Frozen records base then saddle with the ordinary saddle offset and LEQUAL/write/no-cull/no-blend policy; Current executes no Java draw. Completed presentation822 reports2 batches,3 draws,792 vertices and1188 indices under exclusive Rust presentation. The strict crop passes at mean0.02173/max66 under mean0.25/max80. Both clients exit0, reload and validation pass, process memory is complete, all953 Python audit tests pass, and r813 recovery and exact cleanup are verified. The adult/baby chested mule pair is covered; the baby donkey and broader equine combinations remain incomplete. Next unused capture is r814.

The ordinary baby chested/saddled donkey producer passes fresh paired r814. The real server-spawned baby donkey replicates its chest, five storage columns and saddle before opening HorseInventoryScreen. Frozen and Current match every non-tail quad of the base84 and saddle114 models after the declared one-pixel guard; six ordinary age-driven tail quads differ across capture frames while matching exactly between base and saddle layers within each client. Frozen records base then saddle with the ordinary saddle offset and LEQUAL/write/no-cull/no-blend policy; Current executes no Java draw. Completed presentation779 reports2 batches,3 draws,792 vertices and1188 indices under exclusive Rust presentation. The strict crop passes at mean0.03080/max3 under mean0.25/max16. Both clients exit0, reload and validation pass, process memory is complete, all956 Python audit tests pass, and r814 recovery and exact cleanup are verified. The adult/baby chested donkey and mule grid is covered; broader equine combinations remain incomplete. Next unused capture is r815.

The ordinary adult white horse with iron armor and saddle passes fresh paired r816. r815 is retained as a diagnostic: geometry and exclusive ownership were correct, but an unsynchronized random vanilla tail wag occurred only in Current and failed the unchanged pixel gate. The equivalent fixture now resets that random client tail counter to its ordinary non-wagging state before each captured frame in both repositories, without changing renderer behavior. Fresh r816 matches iron armor72 and saddle102 quads; Frozen records iron then saddle at the ordinary offset and Current executes no Java draw. Completed presentation793 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict crop passes at mean0.18638/max66 under mean1/max80. Both clients exit0, reload and validation pass, process memory is complete, all959 Python audit tests pass, and r815/r816 recovery and exact cleanup are verified. Diamond and iron adult equipment are covered; gold, leather, baby material variants and broader equine combinations remain incomplete. Next unused capture is r817.

The ordinary adult white horse with gold armor and saddle passes fresh paired r818. r817 is retained as a command-evidence diagnostic because both clients rendered successfully but the invocation omitted the explicit full-gameplay-attachment request, leaving Current without the required per-frame geometry sidecar. Fresh r818 matches gold armor72 and saddle102 quads; Frozen records gold then saddle at the ordinary offset and Current executes no Java draw. Completed presentation762 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict crop passes at mean0.22934/max66 under mean1/max80. Both clients exit0, reload and validation pass, process memory is complete, all962 Python audit tests pass, and r817/r818 recovery and exact cleanup are verified. Diamond, iron and gold adult equipment are covered; copper, leather, baby material variants and broader equine combinations remain incomplete. Next unused capture is r819.

The ordinary adult white horse with copper armor and saddle passes fresh paired r819. Frozen and Current match copper armor72 and saddle102 quads; Frozen records copper then saddle at the ordinary offset and Current executes no Java draw. Completed presentation856 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict crop passes at mean0.20142/max66 under mean1/max80. Both clients exit0, reload and validation pass, process memory is complete, all965 Python audit tests pass, and r819 recovery and exact cleanup are verified. Diamond, iron, gold and copper adult equipment are covered; leather, baby material variants and broader equine combinations remain incomplete. Next unused capture is r820.

The ordinary adult white horse with default-tint leather armor and saddle passes fresh paired r820. Frozen and Current match leather armor72 and saddle102 quads, and both retain the exact authored tint -6265536; Frozen records leather then saddle at the ordinary offset and Current executes no Java draw. Completed presentation898 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict crop passes at mean0.13245/max3 under mean1/max80. Both clients exit0, reload and validation pass, process memory is complete, all968 Python audit tests pass, and r820 recovery and exact cleanup are verified. The adult white-horse material grid covers diamond, iron, gold, copper and default-tint leather; dyed leather, baby material variants and broader equine combinations remain incomplete. Next unused capture is r821.

The ordinary baby white horse with iron armor and saddle passes fresh paired r821. Frozen and Current match baby base72, iron armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age; Frozen records base then iron and saddle at their ordinary offsets and Current executes no Java draw. Completed presentation836 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict baby crop passes at mean0.05773/max13 under mean0.25/max32. Both clients exit0, reload and validation pass, process memory is complete, all970 Python audit tests pass, and r821 recovery and exact cleanup are verified. Baby diamond and iron equipment are covered; baby gold, copper, leather, dyed leather and broader equine combinations remain incomplete. Next unused capture is r822.

The ordinary baby white horse with gold armor and saddle passes fresh paired r822. Frozen and Current match baby base72, gold armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age; Frozen records base then gold and saddle at their ordinary offsets and Current executes no Java draw. Completed presentation861 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict baby crop passes at mean0.07002/max23 under mean0.25/max32. Both clients exit0, reload and validation pass, process memory is complete, all972 Python audit tests pass, and r822 recovery and exact cleanup are verified. Baby diamond, iron and gold equipment are covered; baby copper, leather, dyed leather and broader equine combinations remain incomplete. Next unused capture is r823.

The ordinary baby white horse with copper armor and saddle passes fresh paired r823. Frozen and Current match baby base72, copper armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age; Frozen records base then copper and saddle at their ordinary offsets and Current executes no Java draw. Completed presentation859 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict baby crop passes at mean0.06136/max19 under mean0.25/max32. Both clients exit0, reload and validation pass, process memory is complete, all974 Python audit tests pass, and r823 recovery and exact cleanup are verified. Baby diamond, iron, gold and copper equipment are covered; baby leather, dyed leather and broader equine combinations remain incomplete. Next unused capture is r824.

The ordinary baby white horse with default-tint leather armor and saddle passes fresh paired r824. Frozen and Current match baby base72, leather armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age, and both retain authored tint -6265536; Frozen records base then leather and saddle at their ordinary offsets and Current executes no Java draw. Completed presentation982 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict baby crop passes at mean0.04299/max3 under mean0.25/max32. Both clients exit0, reload and validation pass, process memory is complete, all976 Python audit tests pass, and r824 recovery and exact cleanup are verified. The baby default material grid covers diamond, iron, gold, copper and default-tint leather; dyed leather and broader equine combinations remain incomplete. Next unused capture is r825.

The ordinary adult white horse with RGB0x3366CC dyed leather armor and saddle passes fresh paired r825. Both clients retain the synchronized item component and signed model tint0xFF3366CC, match leather armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age, and Frozen records the ordinary draw policy while Current executes no Java draw. Completed presentation1063 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict crop passes at mean0.13628/max3 under mean1/max80. Both clients exit0, reload and validation pass, process memory is complete, all979 Python audit tests pass, and r825 recovery and exact cleanup are verified. Adult default materials plus this explicit dye component are covered; baby dyed leather, the wider dye-color space and broader equine combinations remain incomplete. Next unused capture is r826.

The ordinary baby white horse with RGB0x3366CC dyed leather armor and saddle passes fresh paired r826. Both clients retain the synchronized item component and signed model tint0xFF3366CC, match baby base72, leather armor72 and saddle102 quads after the declared one-pixel guard and ordinary non-driving age, and Frozen records the ordinary draw policy while Current executes no Java draw. Completed presentation973 reports3 batches,4 draws,984 vertices and1476 indices under exclusive Rust ownership. The strict baby crop passes at mean0.04253/max3 under mean0.25/max32. Both clients exit0, reload and validation pass, process memory is complete, all981 Python audit tests pass, and r826 recovery and exact cleanup are verified. Adult and baby white horses cover the default armor grid and one explicit dye component; the wider dye-color space and broader equine combinations remain incomplete. Next unused capture is r827.

The ordinary adult black horse with no markings or equipment passes fresh paired r827. The real server-authored and client-replicated state is Variant.BLACK with Markings.NONE, adult age and empty equipment. Frozen and Current match the single horse_black.png 72-quad model after the declared one-pixel guard and ordinary non-driving age; Frozen records one ordinary LEQUAL/write/no-cull/no-blend draw and Current executes no Java draw. Completed presentation927 reports1 batch,2 draws,288 vertices and432 indices under exclusive Rust ownership. The strict crop passes at mean0.02889/max2 under mean2/max4. Both clients exit0, reload and validation pass, process memory is complete, all984 Python audit tests pass, and r827 recovery and exact cleanup are verified. This proves one distinct coat resource beyond white; the remaining coat variants, marking combinations, wider dye-color space and whole-game coverage reconciliation remain incomplete. Next unused capture is r828.

The ordinary adult saddled skeleton horse passes fresh paired r829 after r828 established its strict pixel distribution. The real tamed SkeletonHorse replicates its ordinary saddle slot before opening HorseInventoryScreen. Frozen and Current match the distinct base72 and skeleton-saddle102 models after the declared one-pixel guard and ordinary non-driving age; Frozen records base then saddle with exact ordinary policy and Current executes no Java draw. Completed presentation821 reports2 batches,3 draws,696 vertices and1044 indices under exclusive Rust ownership. The high-contrast thin-cutout gate bounds five metrics; fresh r829 passes at mean0.94128/max137/p9922/142 pixels above64/2977 changed. Both clients exit0, reload and validation pass, process memory is complete, all987 Python audit tests pass, and r828/r829 recovery and exact cleanup are verified. This closes the adult skeleton-horse base+saddle composition; baby skeleton horse, zombie horse and broader entity families remain incomplete. Next unused capture is r830.

The ordinary baby saddled skeleton horse passes fresh paired r831 after r830 exposed a verifier-only draw-order assumption. The real tamed baby SkeletonHorse replicates its ordinary saddle slot before opening HorseInventoryScreen. Frozen and Current match the base72 and skeleton-saddle102 baby models after the declared one-pixel guard and ordinary non-driving age; Frozen proves the baby renderer submits saddle then base with exact ordinary policy, while Current executes no Java draw. Completed presentation771 reports2 batches,3 draws,696 vertices and1044 indices under exclusive Rust ownership. The established skeleton crop gate passes at mean0.24546/max137/p992/33 pixels above64/933 changed. Both clients exit0, reload and validation pass, process memory is complete, all990 Python audit tests pass, and r830/r831 recovery and exact cleanup are verified. This closes the baby skeleton-horse base+saddle composition; zombie horses and broader entity families remain incomplete. Next unused capture is r832.

The ordinary adult saddled zombie horse passes fresh paired r833 after r832 established its distinct low-amplitude pixel distribution. The real tamed adult ZombieHorse replicates its ordinary saddle slot before opening HorseInventoryScreen. Frozen and Current match the zombie base72 and zombie-saddle102 models after the declared one-pixel guard and ordinary non-driving age; Frozen records base then saddle with exact ordinary policy, while Current executes no Java draw. Completed presentation909 reports2 batches,3 draws,696 vertices and1044 indices under exclusive Rust ownership. The zombie-specific crop gate passes at mean0.14378/max49/p992/zero pixels above64/3347 changed. Both clients exit0, reload and validation pass, process memory is complete, all993 Python audit tests pass, and r832/r833 recovery and exact cleanup are verified. This closes the adult zombie-horse base+saddle composition; baby zombie horse and broader entity families remain incomplete. Next unused capture is r834.

The ordinary baby saddled zombie horse passes paired r834. The real tamed baby ZombieHorse replicates its ordinary saddle slot before opening HorseInventoryScreen. Frozen and Current match the zombie base72 and zombie-saddle102 baby models after the declared one-pixel guard and ordinary non-driving age; Frozen proves the baby branch submits saddle then base with exact ordinary policy, while Current executes no Java draw. Completed presentation819 reports2 batches,3 draws,696 vertices and1044 indices under exclusive Rust ownership. The zombie-specific crop gate passes at mean0.04360/max49/p992/zero pixels above64/1061 changed. Both clients exit0, reload and validation pass, process memory is complete, all996 Python audit tests pass, and r834 recovery and exact cleanup are verified. This closes the baby zombie-horse base+saddle composition; broader entity families remain incomplete. Next unused capture is r835.

The ordinary adult unmarked brown horse passes paired r835. The fixture authors and verifies replicated Variant.BROWN with Markings.NONE and empty equipment before opening HorseInventoryScreen. Frozen and Current match the single brown base72 model after the declared one-pixel guard and ordinary non-driving age; Frozen records the exact base draw and Current executes no Java draw. Completed presentation798 reports1 batch,2 draws,288 vertices and432 indices under exclusive Rust ownership. The crop passes at mean0.03432/max2. Both clients exit0, reload and validation pass, process memory is complete, all999 Python audit tests pass, and r835 recovery and exact cleanup are verified. White, black and brown coat identities have scoped acceptance; creamy, chestnut, gray, dark brown, wider markings combinations and broader entity families remain incomplete. Next unused capture is r836.

The ordinary adult creamy-horse r836 proof is implemented but not accepted. The exact fixture requires replicated Variant.CREAMY with Markings.NONE, adult age and empty equipment; geometry requires the single horse_creamy.png 72-quad base and the established exclusive Rust ownership path. Current and Frozen focused Java fixture tests pass14/14 each, focused Python passes652/652, full Python discovery passes1002/1002, and the shared fixture, geometry and test sources are byte-identical. Two paired capture attempts were discarded before acceptance: Frozen could not create a GLX context and Current lost its first-frame drawable. Direct glxinfo reproduces GLXCreateNewContext BadValue on display :0. No r836 capture evidence is retained and r835 remains the latest accepted entity-preview scope. Next unused capture remains r836 after the graphics session recovers.

The adult unmarked saddled Horse world-source grid now covers all seven vanilla coats. Five new scenarios add CHESTNUT, BROWN, BLACK, GRAY and DARK_BROWN to the existing white and creamy fixtures; each requires adult age, tamed state, no markings, empty body armor and the real shared saddle. Current requires the exact coat mesh and shared saddle mesh from the same entity plus completed Rust composition. Frozen records the corresponding two ordinary OpenGL emissions without changing rendering behavior. Validation corrected cloned verifier keys and test names, inverted adult-age receipt serialization, and the model-scenario texture selector that had initially retained white for the new coat scenarios. Both Java trees compile, all406 production contracts pass, and all957 graphics-harness tests pass. These source gates do not replace r836-r839: real paired pixels, exclusive ownership, presentation and bounded-resource evidence remain required once Frozen can create a GLX context on display :0.

The ordinary adult chestnut-horse r837 proof is implemented behind r836 but not accepted. The exact fixture requires a real server-authored and client-replicated Variant.CHESTNUT horse with Markings.NONE, adult age and empty equipment; geometry requires the single horse_chestnut.png 72-quad base plus the established one-model policy and exclusive Rust ownership gates. Current and Frozen focused Java fixture tests pass15/15 each, focused Python passes655/655, full Python discovery passes1005/1005, and shared fixture, geometry and test sources are byte-identical. Frozen changes remain equivalent fixture/read-only observations only. No r837 capture was attempted while Frozen OpenGL is unavailable. r835 remains latest accepted and r836 remains the next unused capture.

The ordinary adult gray r838 and dark-brown r839 proofs are implemented behind r836/r837 but not accepted. Their real fixtures require replicated Variant.GRAY or Variant.DARK_BROWN with Markings.NONE, adult age and empty equipment. Geometry admits only horse_gray.png or horse_darkbrown.png respectively, each with the established 72-quad one-model policy and exclusive Rust ownership gates. Current and Frozen focused Java fixture tests pass17/17 each, focused Python passes661/661, full Python discovery passes1011/1011, and shared fixture, geometry and test sources are byte-identical. Frozen changes remain equivalent fixture/read-only observations only. No r838/r839 captures were attempted while Frozen OpenGL is unavailable. All seven base coats now have strict fixture and verifier support; only white, black and brown currently have paired acceptance. Next unused capture remains r836.

The adult plain-white marking-layer r840 proof is implemented behind r836-r839 but not accepted. Exact horse-marking-white authors and requires a replicated white adult horse with Markings.WHITE and empty equipment. The marked-horse geometry verifier now accepts a marking texture parameter while retaining horse_markings_whitedots.png as its unchanged default; r840 requires horse_markings_white.png and rejects the white-dots layer. The expected base and translucent marking models remain72 quads each with the established two-draw policy and exclusive Rust ownership. Current and Frozen focused Java fixture tests pass18/18 each, focused Python passes664/664, full Python discovery passes1014/1014, and shared fixture, geometry and test sources are byte-identical. Frozen changes remain equivalent fixture/read-only observations only. No r840 capture was attempted while Frozen OpenGL is unavailable. White-field and black-dots marking identities remain to prepare. Next unused capture remains r836.

The adult white-field r841 and black-dots r842 marking-layer proofs are implemented behind r836-r840 but not accepted. Exact fixtures require replicated white adult horses with Markings.WHITE_FIELD or Markings.BLACK_DOTS and empty equipment. Their two-model gates require the white base plus horse_markings_whitefield.png or horse_markings_blackdots.png respectively, the ordinary cutout/translucent draw policies and exclusive Rust ownership; cross-substituted marking state or texture fails closed. Current and Frozen focused Java fixture tests pass20/20 each, focused Python passes670/670, full Python discovery passes1020/1020, and shared fixture, geometry and test sources are byte-identical. Frozen changes remain equivalent fixture/read-only observations only. No r841/r842 capture was attempted while Frozen OpenGL is unavailable. All four marking identities now have strict fixture and verifier support; only white-dots currently has paired acceptance. Next unused capture remains r836.

Inventory layer extraction now copies geometry synchronously after setupAnim(state), carries tint separately from authored order, and uses independent order handles with shared bounded output/rejection state. Only immutable result batches persist after callbacks. Extra overrideCameraAngle model rotation was removed to match ordinary camera semantics. All80 GUI renderer tests pass, including model/state mutation, interleaved order handles and bounds. Current-only r752 completes with exact equipped fixture but visibly differs from inspected Frozenr751: preview pose/lighting differ and exposed Avatar body parts are missing. Original diagnostic and image hashes are preserved;41 complete memory samples and verified recovery/cleanup retained. No inventory acceptance follows. Next require equivalent GUI state/source poses and body geometry plus selected native draw evidence; additionally current enum2 Block lighting differs from vanilla ENTITY_IN_UI light directions and needs an explicit native semantic mode.

Shared read-only inventory source observations are now captured at ordinary InventoryScreen submission with frame-local bounded copied poses/scales/animation, viewport, visibility and skin identity. Both repositories pass19 focused tests, including aliasing/nonfinite/frame-reset/bound rejection. Frozenr753 completed and its image/input receipt was inspected before Current: visible nonspectator WIDE hunglo skin, allouterparts visible, angles206.93408/26.934082/-22.142975, known translation/rotation/scale. Complete45sample memory and verified recovery are retained. Currentr754 comparison is prepared but not launched. Explicit native ENTITY_IN_UI lighting and armor no-cull semantics remain prerequisites; current generic GUI Cutout uses backface culling. No inventory acceptance follows.

Currentr754 proves source inputs were not equivalent: actual head pitch15.707964 vs Frozen-22.142975, body/head angles and both quaternions differ; skin/visibility/viewport/translation/scales match. Animation ages447vs392 also differ. The ordinary InventoryScreen uses prior-frame mouse fields, explaining initial versus subsequent view selection; no Frozen rendering defect is claimed. Shared fixture readiness now waits for matching copied view signatures on distinct frames and resets on screen opening, without overriding input/pose or freezing animation. Both repositories pass20 focused tests, including sameframe rejection, firstview change and stableview with advancingage. r754 original image/input differences and complete memory/recovery evidence are retained. Fresh Frozenr755 is prepared; actual stableview equivalence, animation and native parity remain unproven.

Frozenr755 reports stable source view but retains the zero-mouse angles. This disproves the prior first-frame-only explanation; input equivalence requires explicit fixture positioning. The inspected original frame/receipt,46complete memorysamples and verified recovery are retained. Shared fixture now saves prior cursor, requests ordinary quarter-window then center movement through InputConstants, requires MouseHandler-observed center and stableview, records mouse position/readiness, and restores prior cursor before closing its screen. Both repositories pass20 focused tests. Actual OS delivery and equivalent centered view are unproven until prepared Frozenr756 and Current counterpart run; no renderer acceptance or animation equivalence is claimed.

Frozenr756 delivered only the quarter-window cursor move and correctly refused capture; original metadata/fixture and intentional SIGTERM diagnostic-stop receipt are retained. Complete198sample memory and verified recovery/cleanup retained. Shared fixture now requests center on subsequent frames until MouseHandler observes it. Current ABI60 introduces explicit GUI EntityPreview lighting mode5; Rust supplies vanilla ENTITY_IN_UI normalized directions and preserves signed-i8 normal quantization, while other lighting modes remain unchanged. Entity preview grouping declares this semantic mode. All564 selected Current Java tests and20 Frozen fixture tests pass. Native focused compilation is running; full native and actual centered/pixel/material/ownership proof remain outstanding.

The ABI60 full native suite passes1755tests/2ignored161.12s. Frozenr757 confirms mouse handler center640/360 and stable view readiness, yet saved source angles remain206.93408/26.934082/-22.142975; handlerposition alone cannot prove equivalent preview input. Original image/input/fixture evidence and complete45sample memory plus verified recovery/cleanup are retained. Shared read-only screenMouse tracing now observes supplied and stored coordinates at InventoryScreen background and render-return with a bounded8row frame-local receipt. Current20tests pass; matching Frozentests running, fresh Frozenr758 prepared. No renderer defect conclusion or inventory acceptance follows from the remaining discrepancy.

The baby white unsheared sheep composition proof is implemented but not accepted. Current and Frozen explicitly author and require the same replicated baby/unsheared/white state. Current requires the ordinary sheep.png body plus a same-entity, frame-correlated Rust sheep_wool.png route and minecraft:sheep_wool mesh identity. Review found that both Rust wool callsites placed wool color in the overlay-coordinate slot and forwarded zero as tint. They now preserve vanilla overlay coordinates, put the actual wool color in the tint slot, and record explicit success/unavailable route decisions. The minecraft:sheep plus minecraft:sheep_wool_undercoat composition identity is also registered for the non-white path, which remains unaccepted pending an exact non-white fixture. The focused Current production contract verifies the corrected argument order and route receipts; the prior649-test graphics-harness run remains green. Frozen rendering behavior is unchanged. No paired capture was attempted because display :0 still fails GLX context creation. Real paired pixel, ownership, presentation and bounded-resource evidence remain required. Preserve r836-r842 for the already prepared horse queue and allocate sheep a later identifier after graphics recovery.

The KOB tropical-fish base-and-pattern proof is implemented but remains unaccepted. Both repositories create an exact replicated KOB/orange/white fixture. Frozen passively proves the ordinary tropical_a.png base and tropical_a_pattern_1.png pattern emissions from that same entity. Current requires both Rust routes and copied mesh identities, then correlates exactly one minecraft:tropical_fish base instance and one minecraft:tropical_fish_pattern instance to each accepted capture's completed submission. Fixture, baseline-producer and paired-crop gates fail closed on state, texture, identity, entity, frame, count or submission mismatch. Frozen rendering behavior is unchanged outside the opt-in fixture and passive observation. Both repositories compile, both focused Current layered-model contracts pass, all613 graphics-harness tests pass, and diff checks pass. No paired capture was launched because display :0 still fails GLX context creation; pixel parity, exclusive ownership, presentation and bounded-resource evidence remain outstanding. Horse identifiers r836-r842 stay reserved; allocate tropical fish r843 or later after graphics recovery.

The ordinary spider body-and-eyes proof is implemented but remains unaccepted. Current and Frozen author the same visible replicated spider and correlate its exact spider.png base with spider_eyes.png. Frozen only adds passive OpenGL emission evidence. Current submits the eyes model through the indexed semantic mesh route with minecraft:spider_eyes identity and the dedicated MODEL_EYES material. Rust preserves alpha blending, LEQUAL/no-depth-write raster state, fullbright color and the absence of cardinal model lighting through its existing per-instance semantic lane. The completion gate requires exactly one minecraft:spider base and one minecraft:spider_eyes layer for the same entity in each completed submission, while fixture, texture, route, identity, frame and crop checks reject incomplete or cross-correlated evidence. The focused Rust material/execution test passes, focused Current Java tests pass9/9, all616 graphics-harness tests pass, both repositories compile and diff checks pass. The broad native gate remains unavailable because display :0 fails GLX context creation, so paired pixels, exclusive ownership, presentation and bounded-resource proof are still outstanding. Keep r836-r842 reserved and allocate this scope r843 or later in sequence with the other prepared entities after graphics recovery.

The ordinary Enderman body-and-eyes proof is implemented but remains unaccepted. Current and Frozen author the same visible, calm, empty-handed replicated Enderman and correlate its exact enderman.png base with enderman_eyes.png. Frozen changes are limited to opt-in fixture setup and passive OpenGL emission evidence. Current submits EnderEyesLayer through the indexed semantic mesh route with minecraft:enderman_eyes identity and the shared verified MODEL_EYES fullbright translucent material. The completion gate requires exactly one minecraft:enderman base and one minecraft:enderman_eyes layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame and crop checks reject invisible, creepy, carried-block, missing or cross-correlated evidence. The focused Current Java composition/material tests pass2/2, all619 graphics-harness tests pass, both repositories compile and diff checks pass. No paired capture was attempted while display :0 cannot create a GLX context, so paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary size-zero Phantom body-and-eyes proof is implemented but remains unaccepted. Current and Frozen author the same visible replicated size-zero Phantom and correlate its exact phantom.png base with phantom_eyes.png. Frozen changes are limited to opt-in fixture setup and passive OpenGL emission evidence. Current submits PhantomEyesLayer through the indexed semantic mesh route with minecraft:phantom_eyes identity and the shared verified MODEL_EYES fullbright translucent material. The completion gate requires exactly one minecraft:phantom base and one minecraft:phantom_eyes layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame and crop checks reject invisible, nonzero-size, missing or cross-correlated evidence. The focused Current Java composition/material tests pass2/2, all622 graphics-harness tests pass, both repositories compile and diff checks pass. No paired capture was attempted while display :0 cannot create a GLX context, so paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary active Creaking body-and-eyes proof is implemented but remains unaccepted. Current and Frozen author the same visible, active, non-tearing replicated Creaking and correlate its exact creaking.png base with creaking_eyes.png. Frozen changes are limited to opt-in fixture setup and passive OpenGL emission evidence. Current uses a dedicated CreakingEyesLayer that retains the ordinary OpenGL submission and supplies the Rust Vulkan path with minecraft:creaking_eyes identity and the shared verified MODEL_EYES fullbright translucent material. The completion gate requires exactly one minecraft:creaking base and one minecraft:creaking_eyes layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame and crop checks reject inactive, invisible, tearing-down, missing or cross-correlated evidence. The focused Current Java composition/material tests pass2/2, all625 graphics-harness tests pass, both repositories compile and diff checks pass. No paired capture was attempted while display :0 cannot create a GLX context, so paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary standing Breeze body, scrolling wind, and eyes proof is implemented but remains unaccepted. The previous special Breeze wind route admitted geometry without preserving vanilla's texture-matrix scroll and used generic translucent material semantics. Current now carries a bounded per-instance U offset into Rust and applies it in the built-in model, outline, and shadow programs. Its wind material preserves alpha cutoff 0.1, alpha blending, depth test and writes, lightmapping, and no cardinal diffuse lighting; eyes use the shared fullbright translucent eyes material. Frozen only adds opt-in ordinary entity setup and passive OpenGL observations for breeze.png, breeze_wind.png, and breeze_eyes.png. The harness requires visible standing fixtures, all three exact emissions, routes, and identities, and both body/wind and body/eyes composition receipts on the same completed submission. Focused Rust tests pass2/2, focused Current Java tests pass3/3, four focused Python tests pass, all628 graphics-harness tests pass, and both repositories compile. The unfiltered native suite still terminates at the known host GLX context failure, and the broad Java contract class has two unrelated existing failures. No paired capture was attempted, so real paired pixels, ownership, presentation, and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later after graphics recovery.

The ordinary visible unshorn Bogged body-and-moss proof is implemented but remains unaccepted. Current and Frozen author the same real, visible, unshorn, empty-handed Bogged. Frozen retains ordinary OpenGL rendering and adds only opt-in fixture setup plus passive observations of bogged.png and bogged_overlay.png from the same entity and frame. Current routes the exact unshorn SkeletonClothingLayer overlay through the indexed semantic mesh path with minecraft:bogged_overlay identity and fails closed when the copied mesh is unavailable. The completion gate requires exactly one minecraft:bogged body and one minecraft:bogged_overlay layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame, crop and composition checks reject invisible, sheared, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all631 graphics-harness tests pass, and both repositories compile. Frozen's compile task automatically finalized into a broad suite with16 unrelated existing Java25/Byte Buddy and GL-context failures. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible adult empty-handed Stray body-and-clothing proof is implemented but remains unaccepted. Current and Frozen author the same real Stray and require visible adult state with empty hands. Frozen retains ordinary OpenGL rendering and adds only opt-in fixture setup plus passive observations of stray.png and stray_overlay.png from the same entity and frame. Current routes the exact Stray SkeletonClothingLayer overlay through the indexed semantic mesh path with minecraft:stray_overlay identity and fails closed when the copied mesh is unavailable. The completion gate requires exactly one minecraft:stray body and one minecraft:stray_overlay layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame, crop and composition checks reject invisible, baby, equipped, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all634 graphics-harness tests pass, and both repositories compile. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible adult unequipped Drowned body-and-outer-layer proof is implemented but remains unaccepted. DrownedOuterLayer previously had no whole-frame Rust route. Current now copies the exact cutout outer model through the indexed semantic mesh path with minecraft:drowned_outer identity and fails closed when the mesh is unavailable. Current and Frozen author the same real, visible, adult Drowned with empty hands and head slot. Frozen retains ordinary OpenGL rendering and adds only opt-in setup plus passive observations of drowned.png and drowned_outer_layer.png from the same entity and frame. The completion gate requires exactly one minecraft:drowned body and one minecraft:drowned_outer layer for the same entity and completed submission. Fixture, producer, texture, route, identity, frame, crop and composition checks reject invisible, baby, equipped, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all637 graphics-harness tests pass, and both repositories compile. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible size-two Slime body-and-translucent-outer-shell proof is implemented but remains unaccepted. The previous Rust outer-layer call discarded vanilla's computed overlay coordinate by forwarding -1 and supplied a zero tint. Current now preserves the computed overlay, uses the neutral layer tint, copies the exact translucent shell through the indexed semantic mesh path with minecraft:slime_outer identity, and fails closed when the mesh is unavailable. Current and Frozen author the same real, visible size-two Slime. Frozen retains ordinary OpenGL rendering and adds only opt-in setup plus passive observations that distinguish the shared slime.png texture's cutout body and translucent shell through exact memoized RenderType identity. The completion gate requires exactly one minecraft:slime body and one minecraft:slime_outer shell for the same entity and completed submission. Fixture, producer, route, model identity, frame, crop and composition checks reject invisible, wrong-size, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all640 graphics-harness tests pass, and both repositories compile. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible adult tamed black Cat body-and-red-collar proof is implemented but remains unaccepted. Current copies the exact red-tinted cutout collar model through the indexed semantic mesh path with minecraft:cat_collar identity, preserves vanilla overlay and outline inputs, and fails closed when the mesh is unavailable. Current and Frozen author the same real, visible, adult, tamed Cat with the default black variant and red collar. Frozen retains ordinary OpenGL rendering and adds only opt-in setup plus passive observations of black.png and cat_collar.png from the same entity and frame. The completion gate requires exactly one minecraft:cat body and one minecraft:cat_collar layer for the same entity and completed submission. Fixture, producer, route, texture, identity, frame, crop and composition checks reject invisible, baby, untamed, wrong-color, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all643 graphics-harness tests pass, and both repositories compile. The mandatory broad native task reached the known display :0 GLX context failure, so no paired capture was attempted and real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible adult tamed pale Wolf body-and-red-collar proof is implemented but remains unaccepted. Current copies the exact red-tinted cutout collar through the indexed semantic mesh path with minecraft:wolf_collar identity, preserves vanilla no-overlay and outline inputs, and fails closed when the mesh is unavailable. Its dedicated fixture requires a real adult tamed pale Wolf with a red collar and empty body-armor slot, so wolf-armor evidence cannot satisfy this scope. Frozen retains ordinary OpenGL rendering and adds only opt-in setup plus passive observations of wolf_tame.png and wolf_collar.png from the same entity and frame. The completion gate requires exactly one minecraft:wolf body and one minecraft:wolf_collar layer for the same entity and completed submission. Fixture, producer, route, texture, identity, frame, crop and composition checks reject invisible, baby, untamed, armored, wrong-color, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all646 graphics-harness tests pass, and both repositories compile. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible high-crack Iron Golem body-and-crack-overlay proof is implemented but remains unaccepted. Current copies the exact high-crack cutout overlay through the indexed semantic mesh path with minecraft:iron_golem_cracks identity, preserves vanilla overlay and outline inputs, and fails closed when the mesh is unavailable. Current and Frozen create a real Iron Golem at 20% maximum health and require gameplay to derive Crackiness.Level.HIGH. Frozen retains ordinary OpenGL rendering and adds only opt-in setup plus passive observations of iron_golem.png and iron_golem_crackiness_high.png from the same entity and frame. The completion gate requires exactly one minecraft:iron_golem body and one minecraft:iron_golem_cracks layer for the same entity and completed submission. Fixture, producer, route, texture, identity, frame, crop and composition checks reject invisible, wrong-crackiness, missing or cross-correlated evidence. The focused Current Java contract and four focused Python checks pass, all649 graphics-harness tests pass, and both repositories compile. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later with the other prepared entities after graphics recovery.

The ordinary visible adult red unsheared Sheep body, tinted undercoat, and wool proof is implemented but remains unaccepted. Current and Frozen author the same real adult, red, unsheared Sheep. Frozen retains ordinary OpenGL rendering and passively records exact sheep.png, sheep_wool_undercoat.png, and sheep_wool.png emissions from the same entity, frame, and position. Current routes the two feature models through distinct minecraft:sheep_wool_undercoat and minecraft:sheep_wool identities while preserving vanilla overlay coordinates, wool tint, and outline inputs. The completion gate requires both body/undercoat and body/wool receipts for the same entity and completed submission; fixture, producer, texture, route, identity, frame, position, crop, and composition checks reject baby, sheared, wrong-color, missing, or cross-correlated evidence. Both repositories compile, the focused Current production contract and four focused Python checks pass, and the graphics harness contains 652 tests. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation, and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later after graphics recovery.

The ordinary visible adult plains farmer Villager at level five is implemented but remains unaccepted. Current and Frozen author the same real visible adult plains farmer with empty hands. Frozen retains ordinary OpenGL rendering and passively records villager.png, type/plains.png, profession/farmer.png, and profession_level/diamond.png emissions from the same entity, frame, and position. Current assigns the three feature models distinct minecraft:villager_type, minecraft:villager_profession, and minecraft:villager_profession_level identities and fails closed if any Rust whole-frame semantic mesh is unavailable. The completion gate requires all three body/layer receipts for the same entity and completed submission. Fixture, producer, texture, route, identity, frame, position, crop, and composition checks reject incomplete or cross-correlated evidence. Both repositories compile, the focused Current production contract passes1/1, four focused Python checks pass, and all655 graphics-harness tests pass. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation, and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later after graphics recovery.

The shared Warden/Copper Golem translucent-emissive model prerequisite is implemented but remains unaccepted. Vanilla ENTITY_TRANSLUCENT_EMISSIVE had been classified as an ordinary per-face translucent cutout, which incorrectly retained copied-lightmap modulation and could not distinguish individual overlay models. Current now carries a Rust-owned MODEL_TRANSLUCENT_EMISSIVE identity with alpha blend, cutoff0.1, LEQUAL/no-write depth, no culling, fullbright color and retained cardinal model lighting. The built-in Rust vertex contract uses a separate fullbright-with-cardinal bit rather than Java-side lighting policy. LivingEntityEmissiveLayer supplies explicit semantic identities for all five Warden overlays and Copper Golem eyes, records successful routing, and fails closed under selected whole-frame Vulkan when queuing fails. The ordinary Copper Golem has an exact visible unaffected fixture with empty hands and antenna slot; Frozen correlates its body and eyes by entity, frame and position. The ordinary standing no-AI Warden now uses real client timing plus equivalent capture-only tendril events to cover always-on bioluminescence, both mutually exclusive pulsating phases, tendrils and heartbeat without changing vanilla alpha selection. Frozen pairs each layer with the same-frame body from one stationary entity, while Current requires distinct route/model/identity/correlation and completed-composition evidence across the temporal window. Both repositories compile; the focused Rust material test passes1/1, the Java translucent-raster class passes10/10, the focused production contract passes1/1, four focused Warden harness checks pass, and all661 harness tests pass. Real paired pixels, ownership, presentation and bounded-resource proof remain outstanding for both families. Keep r836-r842 reserved and use r843 or later after graphics recovery.

The ordinary visible adult plains farmer Zombie Villager at level five is implemented but remains unaccepted. Current and Frozen author the same real visible adult, nonconverting, nonaggressive Zombie Villager with empty hands and armor slots. Frozen retains ordinary OpenGL rendering and passively records zombie_villager.png, type/plains.png, profession/farmer.png, and profession_level/diamond.png emissions from the same entity, frame, and position. Current assigns the three feature models distinct minecraft:zombie_villager_type, minecraft:zombie_villager_profession, and minecraft:zombie_villager_profession_level identities and fails closed if a Rust whole-frame semantic mesh is unavailable. The completion gate requires all three body/layer receipts for the same entity and completed submission. Fixture, producer, texture, route, identity, frame, position, crop, and composition checks reject converting, aggressive, baby, equipped, incomplete, or cross-correlated evidence. Both repositories compile, the focused Current production contract passes1/1, four focused Python checks pass, and all664 graphics-harness tests pass. No paired capture was attempted while display :0 cannot create a GLX context, so real paired pixels, exclusive ownership, presentation, and bounded-resource proof remain outstanding. Keep r836-r842 reserved and allocate this scope r843 or later after graphics recovery.

The exact visible-pumpkin Snow Golem body-plus-attached-block source gate is complete but remains unaccepted. The deterministic fixture spawns a real visible Snow Golem with its pumpkin and validates the exact client entity/type/state. Frozen passively records its ordinary body texture and carved-pumpkin block emission from the same entity, frame and position. Current requires the projected carved-pumpkin block display and an actual completed PrimitiveFrame receipt containing one exact Snow Golem body instance plus one exact carved-pumpkin block-material instance in the same submission/capture. The harness requires three correlated frames and exact Current/Frozen fixture equivalence. SnowGolemHeadLayer selects currentBlockDisplayRoute, matching its semantic producer. Current and Frozen compile; focused Python passes4/4, the focused Java source contract passes1/1, full graphics-harness discovery passes668/668, and both trees pass git diff --check. The earlier broad ABI result remains451/454 with its three unrelated pre-existing failures. Display :0 still cannot create a GLX context, so Snow Golem paired pixels, presentation and bounded-resource proof remain incomplete. New entity scopes must use r843 or later behind the reserved r836-r842 horse queue.

Seventeen item-frame source gates are complete but remain unaccepted: ordinary and glow visible and invisible empty frames, diamond-filled and framed-map content; ordinary and glow diamond and map rotation3; an ordinary framed map with one exact minecraft:red_x decoration; invisible ordinary and glow frames with diamond content; and invisible ordinary and glow framed maps. The fake backing states belong to Blocks.AIR and report an invisible render shape, so ItemFrameRenderer submits each resolved BlockStateModel through the material/block-model semantic route with exact minecraft:item_frame or minecraft:glow_item_frame provenance. Current and Frozen author matching real ItemFrame and GlowItemFrame fixtures with explicit visibility, a supporting block and camera-facing orientation. Each filled-item fixture contains exactly minecraft:diamond; its explicit item-frame-item producer carries item and entity identity instead of classifying the mesh as a dropped item. Each map fixture creates real saved map data with a deterministic checkerboard and contains exactly minecraft:filled_map. MapTextureManager stages its 128x128 pixels through the CPU semantic image cache. A scoped map submission carries entity, map, rotation and dynamic minecraft:map/<id> texture identities, and completion evidence requires the exact projected map quad and rotation in the successful Rust material submission. The decorated fixture authors RED_X through the normal map item-component path at map coordinates (24,-16) and decoration rotation5. Its scope records the exact decoration identity and minecraft:textures/atlas/map_decorations.png; completion requires that atlas quad in the same successful Rust submission as the map backing. The invisible fixtures require the backing model to remain absent; empty frames must also emit no item or map content, while diamond and map content use vanilla’s exact 0.5 offset; Current carries visibility and offset through semantic and completed Rust receipts, and Frozen observes the same item or map emission and offset passively. Frozen retains ordinary OpenGL rendering and passively observes the matching backing plus diamond, map or decoration emission and its actual packed light from the same entity. Glow backings require vanilla's minimum block light of five; glow diamonds require fullbright 15/15 and glow maps require vanilla's adjusted 13/15 block/sky light. The representative rotated fixtures use exact vanilla rotation3. Their render-state rotation is recorded at the item/map submission scope and propagated into the completed Rust receipt; the harness rejects a different or missing rotation. It requires three distinct correlated semantic and completion receipts for every branch, preventing reuse across adjacent captures, and exact Current/Frozen fixture equivalence. Current and Frozen compile; the focused item-frame Python checks pass4/4 and Java checks pass3/3, full graphics-harness discovery passes677/677, and both trees pass git diff --check. The broad ABI class passes455/455 after reconciling stale source assertions with the existing generalized hostile-fixture difficulty report, GUI setup-and-observe capture, and state-aware Wither base route. Frozen's compile finalizer remains984/1001 with the same16 Java25/Byte Buddy Mockito failures and one skip. Display :0 still cannot create a GLX context, so paired pixels, exclusive ownership, presentation and bounded-resource proof remain incomplete for all seventeen item-frame scopes; remaining rotation values beyond 3, additional decoration types, other invisible-content combinations remain separate coverage. New entity scopes must use r843 or later behind the reserved r836-r842 horse queue.
