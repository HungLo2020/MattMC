The project’s primary goal is to incrementally and safely migrate MattMC’s entire rendering pipeline from Java to Rust, including OpenGL, Vulkan, shaders, Distant Horizons, and all rendering callsites. Frozen Java OpenGL, maintained in a separate repository, is the authoritative correctness baseline that Current Rust rendering must closely reproduce; Frozen Java Vulkan is not a valid reference, and Frozen may only be changed for deterministic testing, diagnostics, capture, and observability—never rendering behavior—while any suspected Frozen bug must be brought to me before proceeding. We are designing the ideal explicit Rust architecture: semantic callsites feed a Rust-implemented explicit VulkanicGAL, Rust Vulkan consumes explicit GAL state directly, and only the Rust OpenGL backend privately reconstructs OpenGL’s implicit state machine. Parity testing should use the same shared harness across both repositories wherever possible, with explicit repository targeting and equivalent fixtures, world and camera state, packs, timing, diagnostics, and visual captures. Progress must prioritize expanding real Rust-owned rendering coverage through an incremental, safe migration, using rigorous cross-repository parity to catch regressions without allowing validation infrastructure to become the project’s main output. Do not provide a new prompt until I give you an Agent session dump; after each dump, briefly explain what was accomplished and provide a concise, detailed, explicit next prompt of no more than 20 sentences based only on the actual remaining gap.
