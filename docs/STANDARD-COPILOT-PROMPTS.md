Incrementally and safely migrate MattMC’s entire Vulkan rendering pipeline from Java to Rust until selecting Vulkan in the main menu renders the complete game exclusively through semantic rendering callsites, the Rust-implemented explicit VulkanicGAL, and the Rust Vulkan backend, with no Java Vulkan rendering, hidden Java fallback, borrowed Iris GPU state, shared native handles, or second presenter. The completed Rust Vulkan route must support all required rendering systems—including vanilla terrain and fluids, GUI, text, entities, block entities, particles, sky, weather, post-processing, Iris shader packs, Distant Horizons, resource packs, and every remaining rendering callsite—while preserving correct gameplay visuals and behavior. Frozen Java OpenGL in the separate Frozen repository is the authoritative correctness baseline; use the shared cross-repository parity harness with equivalent fixtures, world and camera state, settings, packs, timing, diagnostics, and captures, never use Frozen Java Vulkan as a reference, never change Frozen rendering behavior, and stop for my decision before treating any suspected Frozen behavior as a bug. Preserve the ideal explicit architecture throughout: callsites provide semantic data and commands, VulkanicGAL exposes explicit backend-neutral resources, pipelines, passes, usages, synchronization, submission, completion, and presentation, Rust Vulkan consumes that explicit state directly, and only the Rust OpenGL backend may privately reconstruct OpenGL’s implicit state machine. Work continuously through concrete, tested implementation milestones, prioritize expanding real Rust-owned rendering coverage, keep unfinished capabilities unavailable rather than falsely admitted, and do not stop at audits or prerequisite lists when safe internal implementation progress can be made. Do not use temporary shortcuts that distort the final architecture, do not weaken parity or validation to obtain green results, do not commit or push, and consider the goal complete only when the Vulkan menu option reliably renders the entire supported game through the Rust VulkanicGAL and Rust Vulkan backend with Frozen Java OpenGL parity, clean validation, bounded resources, and no rendering dependency on Java or Iris runtime internals. Treat every missing prerequisite discovered during execution as implementation work within the goal—not as a reason to pause—and continue through safe, compiling, tested intermediate states while keeping incomplete routes private and unadmitted until they are coherent. Prefer narrow, complete vertical migration slices over broad simultaneous coverage expansion. Finish and validate one rendering family or architectural prerequisite before opening another unrelated migration front, unless the latter is strictly required by the current slice. Watch memory usage on host system during runs. Testing has on occasion resulted in the workstation crashing due to memory pressure.

New Prompts:

Incrementally finish MattMC’s core vanilla world rendering through semantic callsites, the explicit Rust VulkanicGAL, and the Rust Vulkan backend. Starting from the current static-terrain/source-execution work, make terrain, all terrain layers, fluids including translucent ordering, world materials/textures, depth, lighting inputs, and required world-pass state execute coherently through Rust with Frozen Java OpenGL parity. Preserve the existing explicit architecture: callsites provide semantic data/commands; VulkanicGAL owns explicit resources, pipelines, passes, usages, synchronization, submission and completion; Rust Vulkan consumes that state directly; only Rust OpenGL may reconstruct implicit GL state. Never borrow Iris GPU/runtime state, share native graphics handles, add Java Vulkan fallback, or create another presenter. Frozen Java OpenGL is the authoritative baseline; use equivalent cross-repository fixtures/camera/settings/timing/captures, never Frozen Java Vulkan, never change Frozen rendering behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Keep incomplete routes unavailable rather than falsely admitted, make safe tested implementation progress instead of stopping at audits, do not weaken validation or use architectural shortcuts, and do not commit or push. Complete this goal only when the ordinary vanilla terrain/fluid world foundation is Rust-owned and parity-proven.

With the core Rust-owned terrain/fluid route established, migrate every remaining vanilla rendering family required for normal gameplay to semantic rendering callsites and the explicit Rust VulkanicGAL/Rust Vulkan backend: GUI and HUD, text, 2D and 3D items, entities, block entities, particles, breaking overlays, selection/borders, sky, clouds, weather, world overlays, post-processing, menus/screens, and other remaining vanilla rendering callsites discovered during execution. Preserve correct ordering, transforms, materials, lighting, blending, depth/stencil, clipping and resource lifetime with Frozen Java OpenGL as the sole correctness baseline. Use the shared cross-repository parity harness with equivalent fixtures, world/camera state, settings, timing, diagnostics and captures; never reference Frozen Java Vulkan, never alter Frozen rendering behavior, and stop for my decision before declaring Frozen behavior buggy. Preserve the explicit architecture and private backend boundary; no Java Vulkan rendering/fallback, Iris-state borrowing, shared native handles, second presenter, producer-specific backend policy, or temporary architecture-distorting shortcuts. Treat missing prerequisites as implementation work, keep incomplete families unadmitted, expand real Rust-owned coverage continuously with compiling/tests/parity evidence, do not weaken validation, and do not commit or push. Finish only when normal vanilla gameplay and UI rendering no longer depend on Java Vulkan rendering.

Complete shader-pack rendering for the Rust Vulkan route without borrowing Iris GPU state or runtime rendering internals. Treat Iris as an integration/source producer only where still necessary while moving the required shader-pack semantics into MattMC’s Rust shader-pack subsystem: source transport/preprocessing, program/pass interpretation, explicit target formats and resources, uniforms/UBOs, samplers, textures, shadow/deferred/composite/final passes, feedback behavior, barriers/layouts, synchronization, ordering and final presentation. VulkanicGAL must express the required operations explicitly and Rust Vulkan must lower them directly; shader-pack policy must remain outside FFI/backends and Rust OpenGL alone may privately reconstruct GL state. Prove behavior against Frozen Java OpenGL using the shared parity harness with equivalent packs, fixtures, camera/world state, settings, timing and captures; never use Frozen Java Vulkan, never modify Frozen rendering, and stop for my decision before classifying Frozen behavior as a bug. Do not fake support by admitting unsupported pack features, borrow Iris-owned GPU objects/state, share native handles, add fallback rendering or weaken validation. Implement missing prerequisites instead of stopping at inventories, proceed through coherent tested internal milestones, do not commit or push, and finish when supported Iris shader packs render through Rust-owned explicit state and the Rust Vulkan backend.

Complete Distant Horizons and resource-pack rendering on the Rust Vulkan route using the same semantic-callsite → explicit VulkanicGAL → Rust Vulkan architecture. Finish DH opaque, transparent and water rendering, exact material/atlas representation, depth and blending behavior, ordering with vanilla terrain and shader-pack passes, required textures/resources, synchronization and lifecycle without borrowing Iris/DH GPU state or native graphics handles. Ensure ordinary resource packs—including altered models, textures, atlases, animation/material behavior and supported rendering semantics—flow through the Rust-owned routes rather than assumptions tied to vanilla assets. Preserve shader-pack compatibility established by the preceding goal. Frozen Java OpenGL remains the authoritative visual/behavioral baseline; use equivalent packs, fixtures, world/camera state, settings, timing, diagnostics and captures through the shared cross-repository harness, never Frozen Java Vulkan, never modify Frozen rendering, and stop for my decision before treating Frozen behavior as erroneous. Fail closed when a visible DH/resource-pack material cannot be represented; do not silently fall back to Java or approximate unsupported semantics. Treat discovered prerequisites as implementation work, keep incomplete capability unadmitted, make continuous tested progress, preserve bounded resources and clean validation, and do not commit or push. Finish when DH and supported resource-pack rendering are Rust-owned and parity-proven.

Close MattMC’s Vulkan migration by proving that selecting settings menu renders the complete supported game exclusively through semantic rendering callsites, the Rust-implemented explicit VulkanicGAL, and the Rust Vulkan backend. Audit every rendering entrypoint and runtime dependency only as a means to remove remaining Java Vulkan execution, hidden Java fallback, borrowed Iris GPU/runtime state, shared native graphics handles, legacy bridge ownership, alternate presentation, or unowned rendering callsites; implement any missing migration work discovered rather than stopping at an audit. Establish exactly one Rust-owned Vulkan device/resource/submission/presentation path with explicit GAL resources, pipelines, passes, usages, synchronization, completion and presentation. Run comprehensive vanilla, GUI/text/entity/block-entity/particle/sky/weather/post-processing, shader-pack, DH and resource-pack parity matrices against Frozen Java OpenGL using equivalent fixtures, world/camera state, settings, packs, timing, diagnostics and captures. Never use Frozen Java Vulkan, never change Frozen rendering behavior, and stop for my decision before treating suspected Frozen behavior as a bug. Keep unsupported capabilities fail-closed; never obtain green results through weakened parity/validation or architectural shortcuts. Require clean Vulkan validation, bounded resources, stable repeated gameplay and final-image parity. Do not commit or push. Complete only when Vulkan has no rendering dependency on Java or Iris runtime internals and Rust owns the sole complete Vulkan renderer and presenter.