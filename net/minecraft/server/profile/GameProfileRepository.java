package net.minecraft.server.profile;

/**
 * Stub for GameProfileRepository - not used in offline mode.
 * Kept for compilation compatibility with legacy code.
 */
public interface GameProfileRepository {
    // Empty stub - offline mode doesn't use profile repositories
}
