#version 120

// Simple fragment shader for shadow depth rendering
// We only need to write depth, color output is disabled

void main() {
    // Depth is automatically written to the depth buffer
    // No need to output color since we disabled color attachments
}
