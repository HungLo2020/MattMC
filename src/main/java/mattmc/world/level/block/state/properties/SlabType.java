package mattmc.world.level.block.state.properties;

/**
 * Enum for slab type (top, bottom, or double).
 * Similar to MattMC's SlabType enum for slabs.
 */
public enum SlabType {
    TOP,
    BOTTOM,
    DOUBLE
}
